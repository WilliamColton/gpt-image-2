---
phase: 06-invite-code
plan: 04
type: execute
wave: 4
depends_on:
  - 06-03
files_modified:
  - src/lib/backendApi.ts
  - src/admin/adminApi.ts
  - src/store.ts
  - src/App.tsx
  - src/components/MigrationModal.tsx
autonomous: true
requirements:
  - D-01
  - D-03
  - D-06
  - D-09
  - D-10
  - D-11
  - D-12
  - D-14
  - D-15
  - D-16
  - D-17
  - D-26
  - D-27
  - D-28
  - D-29

must_haves:
  truths:
    - "backendApi.ts包含loginWithPassword、register、migrate、changePassword、setInviteCode、getInviteCode六个新函数"
    - "adminApi.ts包含adminResetPassword、adminGetInviteConfig、adminUpdateInviteConfig、adminListInvites四个新函数"
    - "AuthUser接口包含username?和needsMigration?可选字段"
    - "bootstrapBackendSession刷新后needsMigration标志在authUser中可用"
    - "App.tsx在authUser.needsMigration为true时显示MigrationModal"
  artifacts:
    - path: "src/lib/backendApi.ts"
      provides: "前端认证API扩展"
      contains: "loginWithPassword"
      contains: "register"
      contains: "migrate"
    - path: "src/admin/adminApi.ts"
      provides: "管理API扩展"
      contains: "adminResetPassword"
      contains: "adminGetInviteConfig"
    - path: "src/store.ts"
      provides: "authUser状态支持needsMigration"
      contains: "needsMigration"
    - path: "src/App.tsx"
      provides: "迁移Modal条件渲染"
      contains: "MigrationModal"
  key_links:
    - from: "loginWithPassword"
      to: "POST /api/auth/login-password"
      via: "request<T> wrapper"
      pattern: "/api/auth/login-password"
    - from: "register"
      to: "POST /api/auth/register"
      via: "request<T> wrapper"
      pattern: "/api/auth/register"
    - from: "App.tsx"
      to: "authUser.needsMigration"
      via: "useStore selector"
      pattern: "needsMigration"
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-invite-code/06-CONTEXT.md
@.planning/phases/06-invite-code/06-UI-SPEC.md

<interfaces>
从现有前端代码库提取的关键接口：

From src/lib/backendApi.ts (现有函数和request模式):
```typescript
function request<T>(path: string, options: RequestInit = {}): Promise<T>  // Bearer token自动注入
export async function loginWithCode(code: string): Promise<{ token: string; user: AuthUser }>  // 参考模式
export function redeemCode(code: string): Promise<{ ok: true; quota?: number; usedCount?: number }>
export function getMe(): Promise<{ user: AuthUser }>
export function getBackendToken(): string
export function setBackendToken(token: string)
export function clearBackendToken()
export interface AuthUser {
  id: string; label: string; role: 'admin' | 'user'; imageCount: number; quota: number; usedCount: number
}
```

From src/admin/adminApi.ts (现有管理API模式):
```typescript
function adminRequest<T>(path: string, options: RequestInit = {}): Promise<T>  // admin Bearer token
export function adminListUsers(): Promise<{ users: AdminUser[] }>
export function adminUpdateAnnouncement(content: string, enabled: boolean): Promise<Announcement>
// 所有admin函数遵循同一模式：adminRequest + 方法 + body JSON.stringify
```

From src/store.ts (现有Zustand状态):
```typescript
export interface AppState {
  authUser: AuthUser | null
  setAuthUser: (user: AuthUser | null) => void
  // ... toast, confirmDialog等
}
export async function bootstrapBackendSession()  // 从getMe获取用户并setAuthUser
export async function logout()  // 清除token和authUser
```

From src/App.tsx (现有条件渲染):
```tsx
{!authUser && <LoginModal />}  // 未登录显示登录弹窗
// 需要新增: {authUser?.needsMigration && <MigrationModal />}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 5: 扩展前端API客户端 — backendApi.ts和adminApi.ts</name>
  <files>src/lib/backendApi.ts, src/admin/adminApi.ts</files>

  <behavior>
    - Test 1: loginWithPassword("user", "pass12345678") calls POST /api/auth/login-password with {username,password}, stores token on success
    - Test 2: register({ username:"newuser", password:"pass12345678", inviteCode:"" }) calls POST /api/auth/register, stores token on success (auto-login)
    - Test 3: migrate({ username, password, confirmPassword }) calls POST /api/auth/migrate with Bearer token
    - Test 4: changePassword({ oldPassword, newPassword, confirmPassword }) calls POST /api/auth/change-password
    - Test 5: adminResetPassword(userId, "newpass") calls PUT /api/admin/users/:id/password
    - Test 6: adminGetInviteConfig() returns {inviterReward, inviteeReward, defaultQuota}
  </behavior>

  <read_first>
    - src/lib/backendApi.ts (现有request<T>模式: token注入、Content-Type设置、错误提取、loginWithCode作为参考模板)
    - src/admin/adminApi.ts (adminRequest<T>模式: admin token管理、adminUpdateAnnouncement作为PUT参考模板)
    - .planning/phases/06-invite-code/06-CONTEXT.md (D-03 login-password端点路径, D-11 register端点路径, D-06 change-password端点路径, D-29 admin端点路径)
    - .planning/phases/06-invite-code/06-UI-SPEC.md (前端组件需要的API函数列表)
  </read_first>

  <action>
  步骤1 — backendApi.ts扩展:
  
  修改AuthUser接口——在role字段后增加:
  - username?: string — 用户名（post-migration有值）
  在usedCount字段后增加:
  - needsMigration?: boolean — password_hash IS NULL时为true
  
  新增6个exported API函数（每个函数风格与loginWithCode保持一致——调用request、存储token、返回结果）:
  
  - loginWithPassword(username: string, password: string): Promise<{ token: string; user: AuthUser; needsMigration: boolean }>
    POST /api/auth/login-password body: {username, password}
    成功后setBackendToken(result.token)再return result
  
  - register(inviteCode: string, username: string, password: string): Promise<{ token: string; user: AuthUser }>
    POST /api/auth/register body: {inviteCode, username, password}
    成功后setBackendToken(result.token)再return result（注册即自动登录）
  
  - migrate(username: string, password: string, confirmPassword: string): Promise<{ user: AuthUser }>
    POST /api/auth/migrate body: {username, password, confirmPassword}
    需要Bearer token（request自动注入）
  
  - changePassword(oldPassword: string, newPassword: string, confirmPassword: string): Promise<{ ok: true }>
    POST /api/auth/change-password body: {oldPassword, newPassword, confirmPassword}
  
  - setInviteCode(code: string): Promise<{ ok: true }>
    PUT /api/auth/invite-code body: {code}
  
  - getInviteCode(): Promise<{ code: string | null; setAt: number | null }>
    GET /api/auth/invite-code

  步骤2 — adminApi.ts扩展:
  
  新增4个exported admin API函数（遵循现有adminRequest模式——POST/PUT时body: JSON.stringify）:
  
  - adminResetPassword(userId: string, password: string): Promise<{ ok: true }>
    PUT /api/admin/users/${encodeURIComponent(userId)}/password body: {password}
  
  - adminGetInviteConfig(): Promise<{ inviterReward: number; inviteeReward: number; defaultQuota: number }>
    GET /api/admin/invite-config
  
  - adminUpdateInviteConfig(inviterReward: number, inviteeReward: number, defaultQuota: number): Promise<{ ok: true; inviterReward: number; inviteeReward: number; defaultQuota: number }>
    PUT /api/admin/invite-config body: {inviterReward, inviteeReward, defaultQuota}
  
  - adminListInvites(): Promise<{ invites: Array<{ username: string; inviteCode: string; usageCount: number }> }>
    GET /api/admin/invites

  注意：每个API函数使用request<T>()或adminRequest<T>()泛型封装，不重复实现token注入逻辑。函数签名与现有loginWithCode风格一致。
  </action>

  <verify>
    <automated>cd /d/Work/BigProject/gpt_image_playground && npx tsc --noEmit --pretty 2>&1 | head -40</automated>
  </verify>

  <acceptance_criteria>
    - src/lib/backendApi.ts中AuthUser接口含username?:string和needsMigration?:boolean字段
    - src/lib/backendApi.ts中export function loginWithPassword(username, password):Promise<{token,user,needsMigration}>
    - src/lib/backendApi.ts中export function register(inviteCode, username, password):Promise<{token,user}>且自动存储token
    - src/lib/backendApi.ts中export function migrate(username, password, confirmPassword):Promise<{user}>
    - src/lib/backendApi.ts中export function changePassword(oldPassword, newPassword, confirmPassword):Promise<{ok:true}>
    - src/lib/backendApi.ts中export function setInviteCode(code):Promise<{ok:true}>
    - src/lib/backendApi.ts中export function getInviteCode():Promise<{code,setAt}>
    - src/admin/adminApi.ts中export function adminResetPassword(userId, password):Promise<{ok:true}>
    - src/admin/adminApi.ts中export function adminGetInviteConfig():Promise<{inviterReward,inviteeReward,defaultQuota}>
    - src/admin/adminApi.ts中export function adminUpdateInviteConfig(...):Promise<{ok,inviterReward,...}>
    - src/admin/adminApi.ts中export function adminListInvites():Promise<{invites}>
    - tsc --noEmit 通过（无类型错误）
  </acceptance_criteria>

  <done>10个新API函数全部实现。AuthUser接口含username和needsMigration。loginWithPassword/register成功时自动存储token。adminApi含全部4个管理端点。TypeScript编译通过。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 6: 更新Zustand store支持needsMigration + App.tsx条件渲染MigrationModal + 占位组件</name>
  <files>src/store.ts, src/App.tsx, src/components/MigrationModal.tsx</files>

  <behavior>
    - Test 1: bootstrapBackendSession() after password login sets authUser with needsMigration !== undefined
    - Test 2: AuthUser类型从backendApi导入的接口包含username和needsMigration字段
    - Test 3: App.tsx在authUser存在且needsMigration===true时渲染MigrationModal（条件性渲染）
    - Test 4: 未登录状态（authUser===null）时MigrationModal不渲染
    - Test 5: tsc --noEmit 通过——类型检查成功（MigrationModal占位组件存在）
  </behavior>

  <read_first>
    - src/store.ts (bootstrapBackendSession函数——从getMe获取user并setAuthUser; AuthUser类型从backendApi导入)
    - src/App.tsx (现有条件渲染模式——{!authUser && <LoginModal />})
    - src/lib/backendApi.ts (Task 5修改后的AuthUser接口)
    - .planning/phases/06-invite-code/06-CONTEXT.md (D-14迁移Modal条件, D-10 needsMigration标志)
    - .planning/phases/06-invite-code/06-UI-SPEC.md (MigrationModal完整规格——Plan 05覆写用)
  </read_first>

  <action>
  步骤1 — 创建MigrationModal占位组件 src/components/MigrationModal.tsx（最小化导出，使tsc编译通过，Plan 05覆写为完整实现）:

  创建文件 src/components/MigrationModal.tsx，内容:
  ```
  export default function MigrationModal() {
    return null
  }
  ```

  此占位保证 import MigrationModal from './components/MigrationModal' 类型检查通过。Jest/vitest mock 时可用此占位。Plan 05 将覆写为完整实现（含不可关闭的Modal、三字段表单、迁移API调用）。

  步骤2 — store.ts修改:
  
  确认store.ts中的import { type AuthUser } from './lib/backendApi'已存在（authUser的类型来源）。
  Task 5已扩展backendApi.ts中的AuthUser接口含username?和needsMigration?字段，
  store.ts中的authUser状态自动获得这些字段（TypeScript类型传播）。
  confirm bootstrapBackendSession()中getMe()返回的user直接通过setAuthUser设置，
  needsMigration字段会通过JSON响应自动填充。无需修改bootstrapBackendSession的逻辑。

  步骤3 — App.tsx修改:
  
  在import区域增加:
  import MigrationModal from './components/MigrationModal'
  
  当前authUser条件渲染逻辑:
  {!authUser && <LoginModal />}
  
  在这行之后增加:
  {authUser?.needsMigration && <MigrationModal />}
  
  authUser.needsMigration为true时（老用户密码登录/兑换码登录后password_hash IS NULL），
  显示MigrationModal（不可关闭的强制迁移弹窗）。
  </action>

  <verify>
    <automated>cd /d/Work/BigProject/gpt_image_playground && npx tsc --noEmit --pretty 2>&1 | head -40</automated>
  </verify>

  <acceptance_criteria>
    - src/components/MigrationModal.tsx 存在，含 export default function MigrationModal() { return null }
    - src/store.ts中AuthUser类型包含username?和needsMigration?字段（通过backendApi导入的类型自动传播）
    - src/App.tsx中import MigrationModal from './components/MigrationModal'
    - src/App.tsx在{!authUser && <LoginModal />}之后包含{authUser?.needsMigration && <MigrationModal />}
    - tsc --noEmit 通过
  </acceptance_criteria>

  <done>MigrationModal占位组件创建。authUser类型含needsMigration。App.tsx在authUser.needsMigration为true时渲染MigrationModal。TypeScript编译通过。Plan 05将覆写占位组件为完整实现。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage | JWT token存储在localStorage中（D-09始终记住登录），loginWithPassword和register成功后自动写入 |
| Bearer token传输 | 所有认证API请求通过request<T>()自动注入Authorization header |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04-01 | Information Disclosure | localStorage JWT | accept | Token在客户端localStorage明文存储（现有模式D-09），XSS风险由React框架缓解。30天过期+登出清除 |
| T-06-04-02 | Tampering | API请求伪造 | mitigate | request<T>()自动注入Bearer token，后端AuthMiddleware验证JWT签名 |
| T-06-04-03 | Elevation of Privilege | adminApi函数 | mitigate | adminRequest<T>()使用独立的ADMIN_TOKEN_KEY存储，AdminMiddleware验证admin角色 |
</threat_model>

<verification>
整体验证：`npx tsc --noEmit` 编译通过。`npm test` 无回归。
</verification>

<success_criteria>
- [x] backendApi.ts包含6个新输入函数（loginWithPassword/register/migrate/changePassword/setInviteCode/getInviteCode）
- [x] adminApi.ts包含4个新管理函数（adminResetPassword/adminGetInviteConfig/adminUpdateInviteConfig/adminListInvites）
- [x] AuthUser接口包含username?和needsMigration?字段
- [x] App.tsx在needsMigration标志为true时渲染MigrationModal
- [x] TypeScript无编译错误
</success_criteria>

<output>
Create .planning/phases/06-invite-code/06-04-SUMMARY.md when done
</output>
