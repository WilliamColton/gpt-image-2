---
phase: 06-invite-code
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend-go/database/models.go
  - backend-go/config/config.go
  - backend-go/service/models.go
  - backend-go/database/database.go
  - backend-go/go.mod
  - backend-go/go.sum
autonomous: true
requirements:
  - D-02
  - D-04
  - D-05
  - D-08
  - D-10
  - D-13
  - D-18
  - D-20
  - D-21

must_haves:
  truths:
    - "User表包含password_hash、username、invite_code、invite_code_set_at、invited_by五个可为NULL的新列"
    - "Config结构体包含inviteInviterReward、inviteInviteeReward、inviteDefaultQuota三个新字段"
    - "AuthUser DTO包含username和needsMigration字段"
    - "GORM AutoMigrate不会对已有用户造成UNIQUE约束冲突"
  artifacts:
    - path: "backend-go/database/models.go"
      provides: "User模型扩展"
      contains: "PasswordHash"
    - path: "backend-go/config/config.go"
      provides: "邀请奖励配置存储"
      contains: "InviteInviterReward"
    - path: "backend-go/service/models.go"
      provides: "AuthUser/User/AdminUser DTO扩展"
      contains: "NeedsMigration"
    - path: "backend-go/database/database.go"
      provides: "AutoMigrate更新"
      contains: "User{}"
  key_links:
    - from: "backend-go/config/config.go"
      to: "config.json"
      via: "persistMu + read-modify-write"
      pattern: "persistMu.Lock"
    - from: "backend-go/database/models.go"
      to: "SQLite unique index"
      via: "uniqueIndex on *string columns"
      pattern: "uniqueIndex"
---

<objective>
建立Phase 06的后端数据基础：扩展User数据库模型（新增password_hash、username、invite_code、invite_code_set_at、invited_by五个可为NULL字段），扩展Config结构体（新增邀请奖励三个配置字段及其getter和持久化函数），扩展服务层DTO（AuthUser/User/AdminUser新增username/needsMigration字段），以及确保bcrypt依赖可用。

Purpose: 所有后续后端任务（service auth逻辑、handler、路由）都依赖这些数据模型和配置定义。必须先于一切其他后端工作完成。

Output: 可编译的后端数据层（models + config + DTOs），go.mod/go.sum含golang.org/x/crypto直接依赖。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-invite-code/06-CONTEXT.md
@.planning/phases/06-invite-code/06-RESEARCH.md

<interfaces>
从现有代码库提取的关键接口和模式：

From backend-go/database/models.go (当前User结构体):
```go
type User struct {
    ID          string `gorm:"primaryKey;type:text"`
    Label       string `gorm:"type:text;not null"`
    Role        string `gorm:"type:text;not null"`
    Status      string `gorm:"type:text;not null;default:active"`
    CreatedAt   int64  `gorm:"not null"`
    LastLoginAt *int64
    Quota       int `gorm:"not null;default:0"`
    UsedCount   int `gorm:"not null;default:0"`
}
```

From backend-go/config/config.go (Config结构体 + persist模式):
```go
type Config struct {
    RootDir     string        `json:"-"`
    DataDir     string        `json:"-"`
    UploadDir   string        `json:"-"`
    Port        int           `json:"port"`
    JWTSecret   string        `json:"jwtSecret"`
    AdminApikey string        `json:"adminApikey"`
    Model       string        `json:"model"`
    APIMode     string        `json:"apiMode"`
    Timeout     int           `json:"timeout"`
    CodexCLI    bool          `json:"codexCli"`
    ApiEndpoints []ApiEndpoint `json:"apiEndpoints"`
    SalePriceX10000 int64     `json:"salePriceX10000"`
}
// persistMu sync.Mutex — 所有config.json写操作必须获取此锁
// persistPricingConfig(eps, salePriceX10000) — 参考模式：read raw → update keys → marshal → write
```

From backend-go/service/models.go (当前AuthUser DTO):
```go
type AuthUser struct {
    ID         string `json:"id"`
    Label      string `json:"label"`
    Role       string `json:"role"`
    ImageCount int    `json:"imageCount"`
    Quota      int    `json:"quota"`
    UsedCount  int    `json:"usedCount"`
}
```

From backend-go/database/database.go (AutoMigrate调用):
```go
DB.AutoMigrate(&User{}, &RedemptionCode{}, &Image{}, &Task{}, &Announcement{}, &Feedback{}, &ChangelogEntry{}, &BillingRecord{})
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 扩展User数据库模型 + 配置扩展 + DTO更新 + bcrypt依赖</name>
  <files>backend-go/database/models.go, backend-go/config/config.go, backend-go/service/models.go, backend-go/database/database.go, backend-go/go.mod, backend-go/go.sum</files>

  <behavior>
    - Test 1: GORM AutoMigrate successfully adds PasswordHash/Username/InviteCode/InviteCodeSetAt/InvitedBy columns to users table without data loss for existing rows
    - Test 2: Multiple users with NULL username can coexist (SQLite uniqueIndex with NULL values)
    - Test 3: Config.App.InviteInviterReward defaults to 0 after Load(), GetInviteInviterReward() returns 0 when config value is 0
    - Test 4: AuthUser JSON serialization includes "username" field when set, includes "needsMigration" when true
    - Test 5: `go build ./...` succeeds after all changes (bcrypt sub-package importable)
  </behavior>

  <read_first>
    - backend-go/database/models.go (当前User模型 — 需新增5个字段)
    - backend-go/config/config.go (当前Config结构体 + persistPricingConfig模式 — 需扩展3个配置字段 + 新增persistInviteConfig)
    - backend-go/service/models.go (当前AuthUser/User/AdminUser DTO — 需扩展username/needsMigration等字段)
    - backend-go/database/database.go (当前AutoMigrate调用 — 无需改动但需确认User{}已在列表中)
    - backend-go/go.mod (当前go依赖 — 确认golang.org/x/crypto是否为indirect)
    - backend-go/go.sum (确认golang.org/x/crypto v0.39.0已存在)
    - .planning/phases/06-invite-code/06-RESEARCH.md (Pattern 2: GORM AutoMigrate nullable columns, Pattern 3: Config persistence, Pattern 4: AuthUser DTO extension)
  </read_first>

  <action>
  步骤1 — bcrypt依赖: 在backend-go/目录执行 `go get golang.org/x/crypto/bcrypt` 将bcrypt子包从indirect提升为direct依赖。执行 `go mod tidy` 清理。验证 `go build ./...` 通过。

  步骤2 — User数据库模型扩展 (database/models.go): 在User结构体的UsedCount字段后新增五个字段：
  - PasswordHash *string (`gorm:"type:text"`) — NULL表示未迁移老用户
  - Username *string (`gorm:"type:text;uniqueIndex"`) — NULL表示未设置用户名，全局唯一
  - InviteCode *string (`gorm:"type:text;uniqueIndex"`) — NULL表示未设置邀请码，全局唯一
  - InviteCodeSetAt *int64 — NULL表示未设置
  - InvitedBy *string (`gorm:"type:text;index"`) — NULL表示非通过邀请码注册，存储注册时使用的邀请码文本（用于ListInvites统计使用次数）
  使用`*string`（指针类型）确保所有新列允许NULL。SQLite的uniqueIndex对NULL值视作不同值，已有用户的安全。

  步骤3 — Config结构体扩展 (config/config.go): 在Config结构体的SalePriceX10000字段后新增三个字段：
  - InviteInviterReward int (`json:"inviteInviterReward"`) — 邀请人奖励配额
  - InviteInviteeReward int (`json:"inviteInviteeReward"`) — 被邀请人奖励配额
  - InviteDefaultQuota int (`json:"inviteDefaultQuota"`) — 无邀请码注册默认配额
  新增三个getter函数（参考GetSalePriceX10000模式，返回类型int）：
  - func GetInviteInviterReward() int — 返回App.InviteInviterReward，若≤0返回0
  - func GetInviteInviteeReward() int — 返回App.InviteInviteeReward，若≤0返回0
  - func GetInviteDefaultQuota() int — 返回App.InviteDefaultQuota
  新增func persistInviteConfig(inviterReward, inviteeReward, defaultQuota int)函数：
  - 获取persistMu锁（与persistPricingConfig共享同一互斥锁）
  - 读取config.json到map[string]json.RawMessage
  - Marshal三个int值，分别写入raw map的inviteInviterReward、inviteInviteeReward、inviteDefaultQuota键
  - json.MarshalIndent + os.WriteFile，参考persistPricingConfig的完整代码模式
  新增func SetInviteConfig(inviterReward, inviteeReward, defaultQuota int)函数：
  - 更新App.InviteInviterReward、App.InviteInviteeReward、App.InviteDefaultQuota
  - 调用persistInviteConfig(inviterReward, inviteeReward, defaultQuota)

  步骤4 — 服务层DTO扩展 (service/models.go):
  在AuthUser结构体中ID字段后新增Username字段：
  - Username string `json:"username,omitempty"` — 用户名用于前端显示
  在AuthUser结构体末尾新增NeedsMigration字段：
  - NeedsMigration bool `json:"needsMigration,omitempty"` — password_hash IS NULL时为true
  在User结构体（服务DTO）中新增：
  - Username string `json:"username,omitempty"`
  - PasswordHash *string `json:"-"` — 永远不序列化到JSON
  - InviteCode *string `json:"inviteCode,omitempty"`
  - InviteCodeSetAt *int64 `json:"-"`
  在AdminUser结构体的Label字段后新增：
  - Username string `json:"username,omitempty"`
  新增辅助函数func dbUserToAuthUser(u *database.User) *AuthUser：
  - Username: 若u.Username不为nil取*u.Username否则为空字符串
  - Label: u.Label
  - NeedsMigration: u.PasswordHash == nil
  - Role/Quota/UsedCount/ID: 直接映射
  - ImageCount填0（由AuthMe等调用路径后填充）

  步骤5 — database/database.go确认: 确认AutoMigrate参数列表中已包含&User{}，无需改动。GORM AutoMigrate发现User结构体新增字段时自动添加对应列。

  步骤6 — 验证: `cd backend-go && go build ./...` 编译通过。`cd backend-go && go vet ./...` 无问题。

  注意：不要在action中写完整函数实现——遵循现有代码模式（persistPricingConfig的read raw→update keys→marshal→write流程，GetSalePriceX10000的简单getter返回模式，以及现有service/models.go的DTO定义模式）。
  </action>

  <verify>
    <automated>cd backend-go && go build ./... && go vet ./...</automated>
  </verify>

  <acceptance_criteria>
    - database/models.go中User结构体包含PasswordHash(*string)、Username(*string,uniqueIndex)、InviteCode(*string,uniqueIndex)、InviteCodeSetAt(*int64)、InvitedBy(*string,index)五个新字段
    - config/config.go中Config结构体包含InviteInviterReward(int)、InviteInviteeReward(int)、InviteDefaultQuota(int)三个字段且json tag不含omitempty
    - config/config.go包含GetInviteInviterReward()/GetInviteInviteeReward()/GetInviteDefaultQuota()三个getter和persistInviteConfig()/SetInviteConfig()两个函数
    - service/models.go中AuthUser含Username(string,json:"username,omitempty")和NeedsMigration(bool,json:"needsMigration,omitempty")字段
    - service/models.go中User含Username/PasswordHash(json:"-")/InviteCode/InviteCodeSetAt(json:"-")字段
    - service/models.go中AdminUser含Username(string,json:"username,omitempty")字段
    - service/models.go含dbUserToAuthUser(u *database.User) *AuthUser函数
    - go.mod中golang.org/x/crypto为direct依赖（非indirect）
    - `go build ./...` 在backend-go目录下返回exit code 0
  </acceptance_criteria>

  <done>后端编译通过。User模型含5个新nullable列（含InvitedBy）。Config含3个邀请配置字段+getter+persist。AuthUser/User/AdminUser DTO含username和needsMigration。dbUserToAuthUser辅助函数可用。bcrypt可直接import。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config.json写入 | 管理员通过AdminMiddleware保护的路由触发persistInviteConfig，写入本地文件系统 |
| SQLite UNIQUE约束 | invite_code和username列的uniqueIndex防止重复，SQLite对NULL值的特殊处理（视为不同）保证已有用户安全 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-01 | Tampering | config.json并发写入 | mitigate | persistInviteConfig获取与persistPricingConfig相同的persistMu互斥锁，防止并发写损坏 |
| T-06-01-02 | Information Disclosure | password_hash列 | mitigate | 数据库存储bcrypt哈希值而非明文；service/models.go中PasswordHash字段json:"-"永不序列化 |
| T-06-01-03 | Elevation of Privilege | Config字段默认值 | accept | 三个邀请奖励字段默认值为0（不奖励），管理员通过受保护的管理API配置后才生效 |
</threat_model>

<verification>
整体验证：`cd backend-go && go build ./... && go vet ./...` 通过。
</verification>

<success_criteria>
- [x] User数据库模型包含password_hash、username、invite_code、invite_code_set_at、invited_by五个可为NULL的新列
- [x] Config结构体包含inviteInviterReward、inviteInviteeReward、inviteDefaultQuota三个新字段及getter/persist
- [x] AuthUser/User/AdminUser DTO包含username和needsMigration字段
- [x] dbUserToAuthUser辅助函数存在
- [x] golang.org/x/crypto为直接依赖
- [x] go build ./... 通过
</success_criteria>

<output>
Create .planning/phases/06-invite-code/06-01-SUMMARY.md when done
</output>
