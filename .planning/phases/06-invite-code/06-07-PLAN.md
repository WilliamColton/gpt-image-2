---
phase: 06-invite-code
plan: 07
type: execute
wave: 6
depends_on:
  - 06-02
  - 06-03
  - 06-04
  - 06-05
  - 06-06
files_modified:
  - backend-go/service/auth_test.go
  - backend-go/handler/auth_test.go
  - backend-go/handler/admin_test.go
  - src/components/LoginModal.test.tsx
  - src/components/MigrationModal.test.tsx
  - src/components/RegisterModal.test.tsx
  - src/lib/backendApi.test.ts
  - src/admin/adminApi.test.ts
autonomous: true
requirements:
  - D-02
  - D-03
  - D-04
  - D-05
  - D-06
  - D-10
  - D-11
  - D-15
  - D-18
  - D-29

must_haves:
  truths:
    - "go test ./... 全部通过（Go后端测试）"
    - "npm test 全部通过（vitest前端测试）"
    - "新测试覆盖所有关键路径——密码哈希、登录、注册、迁移、改密、邀请码、管理API"
  artifacts:
    - path: "backend-go/service/auth_test.go"
      provides: "密码哈希、用户名验证、邀请码唯一性、奖励发放单元测试"
      contains: "TestPasswordHash"
    - path: "backend-go/handler/auth_test.go"
      provides: "密码登录、注册、迁移、改密、邀请码CRUD端点的集成测试"
      contains: "TestPasswordLogin"
    - path: "backend-go/handler/admin_test.go"
      provides: "管理员重置密码、邀请配置GET/PUT、邀请列表管理API测试"
      contains: "TestAdminInvite"
    - path: "src/components/LoginModal.test.tsx"
      provides: "登录弹窗Tab切换、表单验证、错误显示"
    - path: "src/components/MigrationModal.test.tsx"
      provides: "强制迁移弹窗不可关闭、表单验证、提交流程"
    - path: "src/components/RegisterModal.test.tsx"
      provides: "注册弹窗表单验证、邀请码可选性"
  key_links:
    - from: "handler测试"
      to: "httptest.NewRecorder + gin.TestMode"
      via: "标准Go HTTP测试模式"
      pattern: "httptest\\.NewRecorder"
    - from: "vitest测试"
      to: "vi.mock + describe/it/expect"
      via: "vitest + React Testing Library"
      pattern: "vi\\.mock"
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-invite-code/06-RESEARCH.md

<interfaces>
从RESEARCH.md的Validation Architecture部分的测试规范：

Go后端测试模式（参考 backend-go/handler/images_test.go）：
```go
func setupImagesHandlerTest(t *testing.T) *gin.Engine {
    gin.SetMode(gin.TestMode)
    tmp := t.TempDir()
    config.App = &config.Config{DataDir: ..., UploadDir: ..., JWTSecret: "test-secret", AdminApikey: "test-admin"}
    // 创建临时目录 + 打开SQLite内存/文件DB + AutoMigrate + 创建测试数据
    db, _ := gorm.Open(sqlite.Open(filepath.Join(...)), &gorm.Config{})
    database.DB = db
    // 注册路由
    // 返回 gin.Engine 用于 httptest.NewRequest
}
```

前端vitest测试模式（参考 src/lib/db.test.ts）：
```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest'
// vi.mock('../lib/backendApi') 用于mock API调用
// describe('ComponentName', () => { it('behavior description', () => { ... }) })
```

Go handler测试路由注册模式：
```go
r := gin.New()
r.POST("/api/auth/login-password", handler.AuthLoginPassword)
// ... 注册其他路由
req := httptest.NewRequest("POST", "/api/auth/login-password", bodyReader)
req.Header.Set("Content-Type", "application/json")
w := httptest.NewRecorder()
r.ServeHTTP(w, req)
assert.Equal(t, 200, w.Code)
```

现有测试数量：168 vitest tests pass + Go tests pass

RESEARCH.md标注的测试需求：
- backend-go/service/auth_test.go: TestPasswordHash, TestUsernameValidation, TestPasswordValidation
- backend-go/handler/auth_test.go: TestPasswordLogin, TestRegister, TestChangePassword, TestPasswordLoginMigration, TestSetInviteCode, TestRegisterInviteReward
- backend-go/handler/admin_test.go: TestAdminInvite (含admin reset password + invite config + invites list)
- src/components/LoginModal.test.tsx: Tab switching, form validation, error display
- src/components/MigrationModal.test.tsx: Forced non-close, form validation, submit
- src/components/RegisterModal.test.tsx: Form validation, invite code optionality
- src/lib/backendApi.test.ts: new API functions
- src/admin/adminApi.test.ts: new admin API functions
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 10: 创建Go后端单元测试和集成测试</name>
  <files>backend-go/service/auth_test.go, backend-go/handler/auth_test.go, backend-go/handler/admin_test.go</files>

  <behavior>
    - Test 1 (service): bcrypt.GenerateFromPassword生成哈希，CompareHashAndPassword验证通过，错误密码返回false
    - Test 2 (service): username < 3 runes 返回错误 "用户名须为 3-20 个字符"
    - Test 3 (service): password < 8 chars 返回错误 "密码至少需要 8 个字符"
    - Test 4 (handler): POST /api/auth/login-password 正确凭据返回200+{token,user,needsMigration}
    - Test 5 (handler): POST /api/auth/login-password 错误密码返回401
    - Test 6 (handler): POST /api/auth/register 有效数据返回200+{token,user}
    - Test 7 (handler): POST /api/auth/migrate 设置用户名密码成功返回200+{user}
    - Test 8 (handler): POST /api/auth/change-password 正确旧密码返回200+{ok}
    - Test 9 (handler): PUT /api/auth/invite-code 重复码返回400
    - Test 10 (handler): POST /api/auth/register with invite code awards both rewards
    - Test 11 (handler): PUT /api/admin/users/:id/password 返回200+{ok}
    - Test 12 (handler): GET /api/admin/invite-config 返回默认值
    - Test 13 (handler): PUT /api/admin/invite-config 更新成功返回新值
    - Test 14 (handler): GET /api/admin/invites 返回邀请码列表
  </behavior>

  <read_first>
    - backend-go/handler/images_test.go (setup函数模板——gin.TestMode + temp dir + config.App + sqlite + AutoMigrate + 注册路由 + httptest.NewRequest模式)
    - backend-go/service/auth.go (Plan 02创建的所有新函数签名)
    - backend-go/handler/auth.go (Plan 03创建的所有新handler)
    - backend-go/handler/admin.go (Plan 03创建的所有新admin handler)
    - backend-go/config/config.go (getRootDir变量——测试可能需SetRootDir覆盖)
    - backend-go/database/models.go (Plan 01扩展后的User模型)
    - backend-go/middleware/middleware.go (AuthMiddleware——handler测试需要生成JWT)
    - backend-go/main.go (路由注册模式——测试需模拟)
    - .planning/phases/06-invite-code/06-RESEARCH.md (Validation Architecture section——测试命令和测试列表)
  </read_first>

  <action>
  步骤1 — 创建backend-go/service/auth_test.go (单元测试):

  package service_test (外部测试包名)
  导入: testing, golang.org/x/crypto/bcrypt, gorm.io/driver/sqlite, gorm.io/gorm
  导入被测试的包: "gpt-image-playground/backend/service", "gpt-image-playground/backend/config", "gpt-image-playground/backend/database"

  setupServiceTest(t *testing.T):
  - gin.SetMode(gin.TestMode)
  - tmp := t.TempDir()
  - config.App = &config.Config{DataDir: filepath.Join(tmp, "data"), JWTSecret: "test-secret", AdminApikey: "test-admin", ...}
  - os.MkdirAll data/upload dirs
  - db open sqlite memory (filepath.Join(config.App.DataDir, "test.sqlite"))
  - database.DB = db; AutoMigrate User, RedemptionCode
  - 创建测试数据: admin user + test user

  测试函数:
  - TestPasswordHash: bcrypt.GenerateFromPassword → string → bcrypt.CompareHashAndPassword OK + wrong password fail
  - TestPasswordHashNil: 验证hash非空、不相等、非明文
  - TestUsernameValidation: 有效中文用户名(如"张三")通过；太短(2char)报错；太长(21char)报错
  - TestPasswordValidation: <8字符报错；=8字符通过

  步骤2 — 创建backend-go/handler/auth_test.go (集成测试):

  setupAuthHandlerTest(t *testing.T):
  - gin.TestMode + 临时目录 + config.App + SQLite + AutoMigrate + 创建测试用户
  - 创建已迁移用户(user-with-hash: username="existing", password_hash=bcrypt hash of "testpass123")
  - 创建未迁移用户(user-no-hash: password_hash=NULL)
  - 注册gin engine + 所有auth路由 (参考main.go路由注册)
  - 返回gin.Engine

  测试函数（使用httptest.NewRequest + body JSON + ServeHTTP + assert status code + json.Unmarshal assert response body）:
  - TestPasswordLoginSuccess: POST /api/auth/login-password {username:"existing",password:"testpass123"} → 200 + token非空 + user.ID非空 + needsMigration===false
  - TestPasswordLoginWrongPassword: → 401 + error含"用户名或密码错误"
  - TestPasswordLoginNonexistent: → 401 + "用户名或密码错误" (不泄露用户存在性)
  - TestPasswordLoginNoHash: → 400 + 含"尚未设置密码"
  - TestRegisterSuccess: {username:"newuser",password:"abcdefgh",inviteCode:""} → 200 + token非空 + user.username=="newuser"
  - TestRegisterDuplicateUsername: 两次注册同username → 第一次200，第二次400
  - TestRegisterWeakPassword: password="1234567"(7char) → 400
  - TestRegisterWithInviteCode: 先SetInviteCode为owner创建邀请码，再用此码注册 → 新用户quota含奖励 + owner配额增加
  - TestMigrateSuccess: 用JWT认证 → POST /api/auth/migrate → 200 + user.username设置
  - TestMigrateDuplicateUsername: 迁移到已存在的username → 400
  - TestChangePasswordSuccess: 旧密码正确 → POST /api/auth/change-password → 200
  - TestChangePasswordWrongOld: 旧密码错误 → 400
  - TestSetInviteCodeSuccess: PUT /api/auth/invite-code → 200
  - TestSetInviteCodeDuplicate: 两个用户设相同邀请码 → 第二个400
  - TestGetInviteCode: 设置后GET返回code和setAt

  生成JWT helper函数（在测试文件内）:
  func makeAuthHeader(userID string) string { token, _ := service.SignToken(userID, "user", config.App.JWTSecret); return "Bearer "+token }

  步骤3 — 创建/扩展backend-go/handler/admin_test.go:

  setupAdminHandlerTest(t *testing.T):
  - 同上setup + 注册admin路由 + 创建admin用户

  测试函数:
  - TestAdminResetPassword: admin JWT + PUT /api/admin/users/{id}/password → 200
  - TestAdminResetPasswordWeak: password < 8 → 400
  - TestAdminGetInviteConfig: GET /api/admin/invite-config → 200 + 默认值 0
  - TestAdminUpdateInviteConfig: PUT {inviterReward:5,inviteeReward:3,defaultQuota:10} → 200 + 返回值匹配
  - TestAdminListInvites: GET /api/admin/invites → 200 + 数组非null

  步骤4 — 验证: cd backend-go && go test ./service/ -v -count=1 && go test ./handler/ -v -count=1
  </action>

  <verify>
    <automated>cd backend-go && go test ./service/ -v -count=1 2>&1 | tail -20 && go test ./handler/ -v -count=1 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - backend-go/service/auth_test.go存在，含TestPasswordHash、TestPasswordHashNil、TestUsernameValidation、TestPasswordValidation
    - backend-go/handler/auth_test.go存在，含至少10个Test*函数覆盖密码登录、注册、迁移、改密、邀请码全部端点
    - backend-go/handler/admin_test.go存在，含TestAdminResetPassword、TestAdminGetInviteConfig、TestAdminUpdateInviteConfig、TestAdminListInvites
    - 所有handler测试使用httptest.NewRecorder + gin.TestMode + 临时SQLite
    - go test ./service/... 全部通过
    - go test ./handler/... 全部通过
    - 无测试冲突（测试间独立、cleanup清理数据）
  </acceptance_criteria>

  <done>Go单元测试和集成测试全部通过。覆盖密码哈希、登录、注册、迁移、改密、邀请码、管理API。使用临时SQLite和httptest。无测试间冲突。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 11: 创建前端vitest测试——LoginModal、RegisterModal、MigrationModal、API客户端</name>
  <files>src/components/LoginModal.test.tsx, src/components/RegisterModal.test.tsx, src/components/MigrationModal.test.tsx, src/lib/backendApi.test.ts, src/admin/adminApi.test.ts</files>

  <behavior>
    - Test 1 (LoginModal): renders two tabs "兑换码" and "密码登录", default tab is "兑换码"
    - Test 2 (LoginModal): clicking "密码登录" tab switches to password form
    - Test 3 (LoginModal): password form shows username + password inputs + "登录" button
    - Test 4 (LoginModal): "没有邀请码？注册" link is visible
    - Test 5 (RegisterModal): renders three inputs (invite code + username + password) + "注册" button
    - Test 6 (MigrationModal): Escape key does NOT close the modal
    - Test 7 (MigrationModal): backdrop click does NOT close the modal
    - Test 8 (MigrationModal): renders title "设置用户名和密码" + three inputs + "完成设置" button
    - Test 9 (backendApi): loginWithPassword calls correct endpoint
    - Test 10 (backendApi): register calls correct endpoint
  </behavior>

  <read_first>
    - src/lib/db.test.ts (现有vitest测试模式——vi.mock、describe/it/expect、FakeIDB*模式)
    - src/components/LoginModal.tsx (Plan 05改造后的组件——Tab结构)
    - src/components/RegisterModal.tsx (Plan 05创建的组件)
    - src/components/MigrationModal.tsx (Plan 05创建的组件)
    - src/lib/backendApi.ts (Plan 04扩展后的API客户端)
    - src/admin/adminApi.ts (Plan 04扩展后的admin API客户端)
    - src/store.ts (Zustand store——测试中可能需要mock)
    - src/components/ui/tabs.tsx (Radix Tabs封装)
    - .planning/phases/06-invite-code/06-RESEARCH.md (Validation Architecture的测试命令和文件列表)
  </read_first>

  <action>
  步骤1 — 创建src/components/LoginModal.test.tsx:

  使用vitest + @testing-library/react + vi.mock mock backendApi和store。

  setup: vi.mock('../lib/backendApi') mock loginWithCode/loginWithPassword；vi.mock('../store') mock useStore返回mockAuthUser。

  测试:
  - renders two tabs: render(<LoginModal />) → screen.getByText("兑换码") + screen.getByText("密码登录")
  - default tab is code tab: screen.getByPlaceholderText("请输入兑换码") 存在
  - switch to password tab: fireEvent.click(screen.getByText("密码登录")) → screen.getByPlaceholderText("用户名") 存在 + screen.getByPlaceholderText("密码") 存在
  - register link visible: screen.getByText("没有邀请码？注册")
  - empty username shows button disabled: button disabled when input empty
  - error displays: mock loginWithPassword reject → 错误消息可见

  步骤2 — 创建src/components/RegisterModal.test.tsx:

  setup: mock register API + store。

  测试:
  - renders three inputs: invite code(placeholder"选填") + username(placeholder"3-20") + password(placeholder"至少 8 字符")
  - renders register button: screen.getByText("注册") 
  - empty password cannot submit: button disabled或submit后显示错误
  - successful register calls register API: submit → expect(mockRegister).toHaveBeenCalledWith(...)

  步骤3 — 创建src/components/MigrationModal.test.tsx:

  关键测试——不可关闭:
  - renders but escape does NOT close it: render + fireEvent.keyDown(document, { key: 'Escape' }) → modal still in document
  - backdrop click does NOT close: click backdrop → modal still present
  - no X/close button: queryByLabelText("关闭")/queryByRole("button",{name:/close/i}) 为空
  - renders three inputs + submit button: title "设置用户名和密码" + inputs + "完成设置"
  - submit calls migrate API: mockMigrate → success → modal removed

  步骤4 — 扩展src/lib/backendApi.test.ts:

  现有测试文件已有API测试模式。新增:
  - test loginWithPassword calls POST /api/auth/login-password: mock fetch → expect fetch with method POST + body JSON含username/password
  - test register calls POST /api/auth/register: mock fetch → expect body含inviteCode/username/password
  - test migrate calls POST /api/auth/migrate: mock fetch with Bearer token

  步骤5 — 扩展src/admin/adminApi.test.ts:

  新增:
  - test adminResetPassword calls PUT /api/admin/users/{id}/password
  - test adminGetInviteConfig calls GET /api/admin/invite-config
  - test adminUpdateInviteConfig calls PUT body含inviterReward/inviteeReward/defaultQuota
  - test adminListInvites calls GET /api/admin/invites

  验证: npx vitest run
  </action>

  <verify>
    <automated>cd /d/Work/BigProject/gpt_image_playground && npx vitest run 2>&1 | tail -30</automated>
  </verify>

  <acceptance_criteria>
    - src/components/LoginModal.test.tsx含Tab切换测试——默认兑换码+切换到密码登录
    - src/components/RegisterModal.test.tsx含三字段表单渲染和提交测试
    - src/components/MigrationModal.test.tsx含Escape不关闭+backdrop不关闭+无X按钮测试
    - src/lib/backendApi.test.ts含loginWithPassword/register/migrate端点调用测试
    - src/admin/adminApi.test.ts含adminResetPassword/adminGetInviteConfig/adminUpdateInviteConfig/adminListInvites测试
    - npx vitest run 全部通过（无新失败）
    - 现有168个测试通过（无回归）
  </acceptance_criteria>

  <done>前端vitest测试覆盖所有新组件和API函数。MigrationModal不可关闭测试通过。npx vitest run全部通过无回归。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

无——测试代码不涉及生产环境的信任边界。所有测试使用mock/temp数据库。
</threat_model>

<verification>
整体验证：go test ./... 全部通过 + npx vitest run 全部通过。无测试回归。
</verification>

<success_criteria>
- [x] Go backend单元测试——auth_test.go (密码哈希/验证) 和 auth_test.go + admin_test.go (HTTP集成测试)
- [x] Go handler测试覆盖密码登录、注册、迁移、改密、邀请码、管理API
- [x] Frontend vitest测试——LoginModal、RegisterModal、MigrationModal组件测试
- [x] MigrationModal不可关闭行为已测试验证
- [x] API客户端函数已测试（endpoint + HTTP method + body）
- [x] go test ./... 和 npx vitest run 全部通过，无回归
</success_criteria>

<output>
Create .planning/phases/06-invite-code/06-07-SUMMARY.md when done
</output>
