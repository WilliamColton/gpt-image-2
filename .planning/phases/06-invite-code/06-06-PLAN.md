---
phase: 06-invite-code
plan: 06
type: execute
wave: 5
depends_on:
  - 06-04
files_modified:
  - src/admin/AdminDashboard.tsx
autonomous: true
requirements:
  - D-19
  - D-20
  - D-21
  - D-22
  - D-28
  - D-29

must_haves:
  truths:
    - "管理后台新增'邀请码设置'tab——含奖励配置区域和邀请码使用列表区域"
    - "奖励配置区有邀请人奖励配额、被邀请人奖励配额、默认注册配额三个number输入框+保存按钮"
    - "保存按钮调用adminUpdateInviteConfig持久化到config.json"
    - "邀请码使用列表以表格展示——用户、邀请码、使用次数三列"
    - "用户列表每行新增'重置密码'按钮——点击弹出modal输入新密码"
  artifacts:
    - path: "src/admin/AdminDashboard.tsx"
      provides: "管理后台邀请码tab + 用户重置密码modal"
      contains: "'invites'"
      contains: "adminResetPassword"
      contains: "adminGetInviteConfig"
  key_links:
    - from: "邀请码tab"
      to: "adminApi.ts adminUpdateInviteConfig"
      via: "保存按钮的handleSaveInviteConfig"
      pattern: "adminUpdateInviteConfig"
    - from: "重置密码modal"
      to: "adminApi.ts adminResetPassword"
      via: "确认按钮的handleResetPassword"
      pattern: "adminResetPassword"
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-invite-code/06-CONTEXT.md
@.planning/phases/06-invite-code/06-UI-SPEC.md

<interfaces>
从06-UI-SPEC.md锁定的Admin Dashboard视觉约定：

Tab类型新增 'invites'：
```
<button className={`rounded-xl px-4 py-2 text-sm font-medium transition ${tab === 'invites' ? 'bg-blue-600 text-white' : 'bg-white/5 text-gray-400 hover:bg-white/10'}`}>
  邀请码设置
</button>
```

奖励配置区（Admin特有的dark-only主题）：
```
<div className="rounded-2xl border border-white/10 bg-white/[0.03] p-5">
  <h3 className="text-sm font-medium text-gray-200 mb-4">奖励配置</h3>
  <div className="flex flex-wrap items-end gap-4">
    <div><label>邀请人奖励配额（张）</label><input type="number" min="0" className="w-36 rounded-lg border border-white/10 bg-white/[0.04] px-3 py-2 text-sm text-gray-100" /></div>
    <div><label>被邀请人奖励配额（张）</label><input type="number" min="0" /></div>
    <div><label>默认注册配额（张）</label><input type="number" min="0" /></div>
    <button className="bg-green-600">保存配置</button>
  </div>
</div>
```

邀请码使用列表区：
```
<div><h3>邀请码使用情况</h3>
<table><thead><tr><th>用户</th><th>邀请码</th><th>使用次数</th></tr></thead>
<tbody>rows</tbody></table>
</div>
```

重置密码modal（与现有quotaModal/confirmModal同级样式）：
```
<div className="fixed inset-0 z-50 flex items-center justify-center p-4">
  <div className="absolute inset-0 bg-black/50" onClick={close} />
  <div className="relative z-10 w-full max-w-sm rounded-2xl border border-white/10 bg-gray-900 p-5 shadow-2xl">
    <h3>重置密码 -- {label}</h3>
    <input type="password" placeholder="输入新密码（至少 8 字符）" />
    <button>取消</button><button className="bg-blue-600">确认</button>
  </div>
</div>
```

可用API函数 (Plan 04/task 5创建的adminApi函数):
- adminResetPassword(userId: string, password: string): Promise<{ ok: true }>
- adminGetInviteConfig(): Promise<{ inviterReward: number; inviteeReward: number; defaultQuota: number }>
- adminUpdateInviteConfig(inviterReward: number, inviteeReward: number, defaultQuota: number): Promise<{ ok: true; ... }>
- adminListInvites(): Promise<{ invites: Array<{ username: string; inviteCode: string; usageCount: number }> }>

现有AdminDashboard.tsx的状态变量模式和Tab切换结构：
```typescript
type Tab = 'users' | 'codes' | 'config' | 'analytics' | 'announcement' | 'feedback' | 'changelog'
// 需扩展为: 'users' | 'codes' | 'config' | 'analytics' | 'announcement' | 'feedback' | 'changelog' | 'invites'
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 9: 新增管理后台邀请码设置tab + 用户重置密码功能</name>
  <files>src/admin/AdminDashboard.tsx</files>

  <behavior>
    - Test 1: 点击"邀请码设置"tab切换到invites tab内容
    - Test 2: invites tab显示奖励配置区——三个number输入(邀请人/被邀请人/默认配额)+"保存配置"按钮
    - Test 3: "保存配置"按钮调用adminUpdateInviteConfig并toast成功
    - Test 4: invites tab显示邀请码使用情况表格——用户/邀请码/使用次数
    - Test 5: 用户列表每行有"重置密码"按钮
    - Test 6: 点击"重置密码"弹出modal——显示用户名+密码输入框+取消/确认按钮
    - Test 7: 确认按钮调用adminResetPassword，成功后toast+关闭modal
  </behavior>

  <read_first>
    - src/admin/AdminDashboard.tsx (当前完整实现——Tab结构、状态变量、loadUsers/loadCodes/loadXxx模式、quotaModal/confirmModal模式、table渲染)
    - src/admin/adminApi.ts (Plan 04/task 5新增的adminResetPassword/adminGetInviteConfig/adminUpdateInviteConfig/adminListInvites)
    - .planning/phases/06-invite-code/06-UI-SPEC.md (锁定: Admin invites tab UI规范、重置密码modal UI规范、copywriting)
  </read_first>

  <action>
  步骤1 — 扩展类型和导入:

  在已有的import中新增:
  - import { adminResetPassword, adminGetInviteConfig, adminUpdateInviteConfig, adminListInvites } from './adminApi'

  扩展Tab类型:
  - type Tab = 'users' | 'codes' | 'config' | 'analytics' | 'announcement' | 'feedback' | 'changelog' | 'invites' (末尾新增'invites')

  步骤2 — 新增invite相关状态变量（在其他状态变量附近）:

  - const [inviterReward, setInviterReward] = useState(0)
  - const [inviteeReward, setInviteeReward] = useState(0)
  - const [defaultQuota, setDefaultQuota] = useState(0)
  - const [inviteConfigLoading, setInviteConfigLoading] = useState(false)
  - const [inviteConfigSaving, setInviteConfigSaving] = useState(false)
  - const [inviteRows, setInviteRows] = useState<Array<{username:string;inviteCode:string;usageCount:number}>>([])
  - const [inviteRowsLoading, setInviteRowsLoading] = useState(false)
  - const [resetPasswordModal, setResetPasswordModal] = useState<{userId:string;label:string} | null>(null)
  - const [resetPasswordValue, setResetPasswordValue] = useState('')

  步骤3 — 新增数据加载函数:

  - loadInviteConfig (useCallback):
    setInviteConfigLoading(true) → try { const cfg = await adminGetInviteConfig(); setInviterReward(cfg.inviterReward); setInviteeReward(cfg.inviteeReward); setDefaultQuota(cfg.defaultQuota) } catch { toast } finally { setInviteConfigLoading(false) }

  - loadInviteRows (useCallback):
    setInviteRowsLoading(true) → try { const { invites } = await adminListInvites(); setInviteRows(invites || []) } catch { toast } finally { setInviteRowsLoading(false) }

  步骤4 — 扩展useEffect tab切换逻辑:

  在现有的useEffect链中（if (tab === 'users') ... else if ...）增加:
  - else if (tab === 'invites') { loadInviteConfig(); loadInviteRows() }

  步骤5 — 新增保存配置处理函数:

  - handleSaveInviteConfig: 调用 adminUpdateInviteConfig(inviterReward, inviteeReward, defaultQuota)，setInviteConfigSaving包裹，成功后toast('配置已保存', 'success')

  步骤6 — 新增重置密码处理函数:

  - handleResetPassword: 验证resetPasswordValue长度>=8否则toast错误
  - 调用 adminResetPassword(resetPasswordModal!.userId, resetPasswordValue.trim())
  - 成功后toast('密码已重置', 'success') + setResetPasswordModal(null) + setResetPasswordValue('')

  步骤7 — 在tab导航栏新增按钮（在"更新日志"按钮之后）:

  ```
  <button onClick={() => { setTab('invites'); ...清除选择 }}
    className={`rounded-xl px-4 py-2 text-sm font-medium transition ${tab === 'invites' ? 'bg-blue-600 text-white' : 'bg-white/5 text-gray-400 hover:bg-white/10'}`}>
    邀请码设置
  </button>
  ```

  步骤8 — 在main区域内新增invites tab内容（在最后一个tab === 'changelog'的})}之后，</main>之前）:

  {tab === 'invites' && (
    <div className="space-y-5">
      {/* 奖励配置区 */}
      <div className="rounded-2xl border border-white/10 bg-white/[0.03] p-5">
        <h3 className="text-sm font-medium text-gray-200 mb-4">奖励配置</h3>
        {inviteConfigLoading ? (<div>加载中...</div>) : (
          <div className="flex flex-wrap items-end gap-4">
            <div>
              <label className="block text-xs text-gray-500 mb-1">邀请人奖励配额（张）</label>
              <input type="number" min="0" value={inviterReward} onChange={e => setInviterReward(parseInt(e.target.value)||0)}
                className="w-36 rounded-lg border border-white/10 bg-white/[0.04] px-3 py-2 text-sm text-gray-100 outline-none focus:border-blue-400" />
            </div>
            <div>
              <label className="block text-xs text-gray-500 mb-1">被邀请人奖励配额（张）</label>
              <input type="number" min="0" value={inviteeReward} onChange={e => setInviteeReward(parseInt(e.target.value)||0)}
                className="w-36 rounded-lg border border-white/10 bg-white/[0.04] px-3 py-2 text-sm text-gray-100 outline-none focus:border-blue-400" />
            </div>
            <div>
              <label className="block text-xs text-gray-500 mb-1">默认注册配额（张）</label>
              <input type="number" min="0" value={defaultQuota} onChange={e => setDefaultQuota(parseInt(e.target.value)||0)}
                className="w-36 rounded-lg border border-white/10 bg-white/[0.04] px-3 py-2 text-sm text-gray-100 outline-none focus:border-blue-400" />
            </div>
            <button onClick={handleSaveInviteConfig} disabled={inviteConfigSaving}
              className="rounded-xl bg-green-600 px-4 py-2 text-sm font-medium text-white transition hover:bg-green-700 disabled:cursor-not-allowed disabled:opacity-50">
              {inviteConfigSaving ? '保存中...' : '保存配置'}
            </button>
          </div>
        )}
      </div>

      {/* 邀请码使用列表区 */}
      <div className="rounded-2xl border border-white/10 bg-white/[0.03] p-5">
        <h3 className="text-sm font-medium text-gray-200 mb-4">邀请码使用情况</h3>
        {inviteRowsLoading ? (<div>加载中...</div>) : inviteRows.length === 0 ? (
          <div className="py-12 text-center text-gray-500">暂无邀请码使用记录</div>
        ) : (
          <div className="overflow-x-auto">
            <table className="w-full text-sm">
              <thead>
                <tr className="border-b border-white/10 text-left text-gray-400">
                  <th className="px-4 py-3 font-medium text-xs">用户</th>
                  <th className="px-4 py-3 font-medium text-xs">邀请码</th>
                  <th className="px-4 py-3 font-medium text-xs text-right">使用次数</th>
                </tr>
              </thead>
              <tbody>
                {inviteRows.map((row, i) => (
                  <tr key={i} className="border-b border-white/5 hover:bg-white/[0.02]">
                    <td className="px-4 py-3 text-sm text-gray-300">{row.username}</td>
                    <td className="px-4 py-3"><span className="font-mono text-xs bg-white/5 px-2 py-0.5 rounded">{row.inviteCode || '-'}</span></td>
                    <td className="px-4 py-3 text-sm text-right tabular-nums text-gray-300">{row.usageCount}</td>
                  </tr>
                ))}
              </tbody>
            </table>
          </div>
        )}
      </div>
    </div>
  )}

  步骤9 — 在用户表格的"操作"列（现有<button>启用/禁用</button><button>删除</button>）之前新增"重置密码"按钮:

  在每个用户行(<tr key={user.id}>)的操作td中，现有两个按钮之前添加:
  ```
  <button onClick={() => { setResetPasswordModal({ userId: user.id, label: user.label }); setResetPasswordValue('') }}
    className="rounded-lg bg-blue-600/20 px-3 py-1 text-xs font-medium text-blue-400 hover:bg-blue-600/30">
    重置密码
  </button>
  ```

  步骤10 — 在AdminDashboard return的结尾（Toast组件之前）新增resetPasswordModal:

  参考现有quotaModal的JSX结构（fixed inset-0 z-50 + backdrop + panel），在最后一个modal（batchConfirm）之后、<Toast />之前添加:
  ```
  {resetPasswordModal && (
    <div className="fixed inset-0 z-50 flex items-center justify-center p-4">
      <div className="absolute inset-0 bg-black/50" onClick={() => setResetPasswordModal(null)} />
      <div className="relative z-10 w-full max-w-sm rounded-2xl border border-white/10 bg-gray-900 p-5 shadow-2xl">
        <h3 className="text-sm font-semibold text-gray-100 mb-4">重置密码 — {resetPasswordModal.label}</h3>
        <input type="password" value={resetPasswordValue} onChange={e => setResetPasswordValue(e.target.value)}
          placeholder="输入新密码（至少 8 字符）" autoFocus
          className="w-full rounded-xl border border-white/10 bg-white/[0.04] px-3 py-2.5 text-sm text-gray-100 outline-none focus:border-blue-400 mb-4" />
        <div className="flex justify-end gap-2">
          <button onClick={() => setResetPasswordModal(null)} className="rounded-xl px-4 py-2 text-sm text-gray-400 hover:bg-white/5">取消</button>
          <button onClick={handleResetPassword} disabled={resetPasswordValue.length < 8}
            className="rounded-xl bg-blue-600 px-4 py-2 text-sm font-medium text-white hover:bg-blue-700 disabled:cursor-not-allowed disabled:opacity-50">确认</button>
        </div>
      </div>
    </div>
  )}
  ```

  所有UI规范、颜色、spacing严格按照06-UI-SPEC.md。admin区域使用dark-only主题。copywriting完全一致（标签文本、按钮文本、placeholder）。
  </action>

  <verify>
    <automated>cd /d/Work/BigProject/gpt_image_playground && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>

  <acceptance_criteria>
    - AdminDashboard.tsx的Tab类型扩展为含'invites'
    - tab导航栏含"邀请码设置"按钮（8个tab按钮）
    - invites tab含"奖励配置"section——三个number输入+绿色"保存配置"按钮
    - "保存配置"调用adminUpdateInviteConfig，成功后toast "配置已保存"
    - invites tab含"邀请码使用情况"section——表格(用户/邀请码/使用次数)
    - 空数据时显示"暂无邀请码使用记录"
    - 用户列表每行"重置密码"按钮在"启用/禁用"和"删除"按钮之前
    - 重置密码modal含标题"重置密码 -- {label}"+password input+"取消"/"确认"按钮
    - 确认按钮在密码<8字符时disabled，成功后toast "密码已重置"
    - 所有admin区域使用dark-only主题和border-white/10等约定样式
    - tsc --noEmit 通过
  </acceptance_criteria>

  <done>Admin Dashboard新增invites tab——奖励配置(3个input+保存按钮)+邀请码使用列表(table)。用户列表每行新增重置密码按钮+在线modal。TypeScript编译通过。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 管理后台 | 所有adminApi调用通过adminRequest<T>()发送Bearer admin token，后端AdminMiddleware验证admin角色 |
| 重置密码modal | 管理员输入新密码——不需要旧密码（D-06后段） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06-01 | Elevation of Privilege | 邀请配置修改 | mitigate | adminUpdateInviteConfig通过adminRequest发送——后端AdminMiddleware验证admin角色，config.json通过persistMu保护 |
| T-06-06-02 | Information Disclosure | resetPasswordModal | mitigate | 输入框type="password"防止旁观者窥视；modal通过z-50覆盖在admin页面内容之上 |
</threat_model>

<verification>
整体验证：`npx tsc --noEmit` 编译通过。管理后台手动验证invites tab功能正常。
</verification>

<success_criteria>
- [x] AdminDashboard含'invites'tab——奖励配置+邀请码列表两区
- [x] 奖励配置——三个number输入（邀请人/被邀请人/默认配额）+保存按钮
- [x] 保存按钮调用adminUpdateInviteConfig，toast成功反馈
- [x] 邀请码列表——表格三列（用户/邀请码/使用次数），空数据提示
- [x] 用户列表每行"重置密码"按钮+弹出modal+密码输入
- [x] 重置密码modal验证密码>=8字符，确认按钮disabled直到满足条件
- [x] 所有UI遵循06-UI-SPEC.md的视觉约定和copywriting
- [x] TypeScript无编译错误
</success_criteria>

<output>
Create .planning/phases/06-invite-code/06-06-SUMMARY.md when done
</output>
