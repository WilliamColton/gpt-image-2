---
phase: 05-cost-revenue-analytics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend-go/service/money.go
  - backend-go/service/money_test.go
  - backend-go/database/models.go
  - backend-go/database/database.go
  - backend-go/service/billing.go
  - backend-go/service/billing_test.go
autonomous: true
requirements:
  - COST-03
  - COST-04
must_haves:
  truths:
    - "D-12: 系统存在独立 billing_records 表保存成功图片记账明细"
    - "D-07: 金额以 X10000 固定点整数保存，最多 4 位小数"
    - "D-13: 账单记录保留 task/user/endpoint/price 快照，不依赖任务或用户外键; D-14: deleting task/user must not delete historical cost/revenue stats"
  artifacts:
    - path: "backend-go/service/money.go"
      provides: "MoneyScale=10000 and fixed-point parse/format helpers"
      exports: ["MoneyScale", "ParseMoneyX10000", "FormatMoneyX10000"]
    - path: "backend-go/database/models.go"
      provides: "BillingRecord GORM model"
      contains: "func (BillingRecord) TableName() string { return \"billing_records\" }"
    - path: "backend-go/service/billing.go"
      provides: "billing write service for successful saved images"
      exports: ["BillingImageInput", "BillingBatchInput", "RecordBillingForSuccessfulImages"]
  key_links:
    - from: "backend-go/database/database.go"
      to: "backend-go/database/models.go"
      via: "AutoMigrate includes &BillingRecord{}"
      pattern: 'AutoMigrate\(.*BillingRecord'
    - from: "backend-go/service/billing.go"
      to: "backend-go/database/models.go"
      via: "database.DB.Create(&records) writes BillingRecord rows"
      pattern: 'database\.DB\.Create\(&records\)'
---

<objective>
建立成本收益记账的后端基础：固定点金额 helper、独立账单表、按成功保存图片事件写入账单的 service。

Purpose: 支撑 D-01、D-02、D-06、D-07、D-12、D-13、D-14，保证后续生成流程和统计 API 都从同一份历史快照账单读取。
Output: `billing_records` 数据模型、AutoMigrate 接入、金额解析/格式化 helper、账单写入 service 和单元测试。
</objective>

<execution_context>
@C:/Users/32478/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/32478/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-cost-revenue-analytics/05-CONTEXT.md
@.planning/phases/05-cost-revenue-analytics/05-RESEARCH.md
@backend-go/database/models.go
@backend-go/database/database.go
@backend-go/service/image_test.go
@backend-go/util/id.go

<interfaces>
Existing backend patterns to preserve:
- GORM models live in `backend-go/database/models.go` and declare `TableName()` methods.
- Database initialization uses `database.DB.AutoMigrate(...)` in `backend-go/database/database.go`.
- Service-layer tests set `config.App`, assign `database.DB`, and run `AutoMigrate` against a temporary SQLite database.
- Billing records must not use GORM foreign-key constraints to tasks or users, per D-14.
- Existing ID generation uses `backend-go/util.GenerateID()`; billing writes must assign one non-empty unique ID per `BillingRecord` row.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add fixed-point money helper</name>
  <files>backend-go/service/money.go, backend-go/service/money_test.go</files>
  <read_first>
    - .planning/phases/05-cost-revenue-analytics/05-CONTEXT.md
    - .planning/phases/05-cost-revenue-analytics/05-RESEARCH.md
    - backend-go/service/models.go
    - backend-go/service/image_test.go
  </read_first>
  <behavior>
    - `ParseMoneyX10000("0")` returns `0`.
    - `ParseMoneyX10000("12.3456")` returns `123456`.
    - `ParseMoneyX10000("12.34567")`, `ParseMoneyX10000("")`, `ParseMoneyX10000("-0.0001")`, and `ParseMoneyX10000("abc")` return errors.
    - `FormatMoneyX10000(123400)` returns `"12.34"`; `FormatMoneyX10000(10000)` returns `"1"`; `FormatMoneyX10000(1)` returns `"0.0001"`.
  </behavior>
  <action>Create `backend-go/service/money.go` with `const MoneyScale int64 = 10000`, `func ParseMoneyX10000(input string) (int64, error)`, and `func FormatMoneyX10000(value int64) string`. Implement parsing with string trimming, digit checks, and base-10 integer arithmetic only; do not use `float32`, `float64`, `strconv.ParseFloat`, or multiplication of floating values. Reject empty, negative, non-numeric, and more-than-4-decimal inputs per D-07. Create `backend-go/service/money_test.go` covering the behavior cases above.</action>
  <verify>
    <automated>cd backend-go && go test ./service/... -run 'Test(ParseMoneyX10000|FormatMoneyX10000)'</automated>
  </verify>
  <acceptance_criteria>
    - `backend-go/service/money.go` contains `const MoneyScale int64 = 10000`.
    - `backend-go/service/money.go` contains `func ParseMoneyX10000(input string) (int64, error)`.
    - `backend-go/service/money.go` contains `func FormatMoneyX10000(value int64) string`.
    - `backend-go/service/money.go` does not contain `ParseFloat`, `float32`, or `float64`.
    - `cd backend-go && go test ./service/... -run 'Test(ParseMoneyX10000|FormatMoneyX10000)'` exits 0.
  </acceptance_criteria>
  <done>Fixed-point money parsing and formatting is tested and available for backend config, billing, and analytics code.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add independent billing record model and migration</name>
  <files>backend-go/database/models.go, backend-go/database/database.go</files>
  <read_first>
    - .planning/phases/05-cost-revenue-analytics/05-CONTEXT.md
    - .planning/phases/05-cost-revenue-analytics/05-RESEARCH.md
    - backend-go/database/models.go
    - backend-go/database/database.go
  </read_first>
  <behavior>
    - AutoMigrate creates a `billing_records` table.
    - The billing model contains snapshot columns for `task_id`, `user_id`, `user_label_snapshot`, `endpoint_base_url_snapshot`, `success_image_count`, `unit_cost_x10000`, `unit_sale_x10000`, `cost_x10000`, `revenue_x10000`, `profit_x10000`, and `created_at`.
    - Deleting a user or task does not cascade-delete billing rows because no GORM foreign-key relation is declared on `BillingRecord`.
  </behavior>
  <action>Add `BillingRecord` to `backend-go/database/models.go` with fields `ID string`, `TaskID string`, `UserID string`, `UserLabelSnapshot string`, `EndpointBaseURLSnapshot string`, `OutputImageID string`, `SuccessImageCount int`, `UnitCostX10000 int64`, `UnitSaleX10000 int64`, `CostX10000 int64`, `RevenueX10000 int64`, `ProfitX10000 int64`, and `CreatedAt int64`. Use text/not-null/index GORM tags where appropriate, with indexes on `task_id`, `user_id`, `endpoint_base_url_snapshot`, and `created_at`. Add `func (BillingRecord) TableName() string { return "billing_records" }`. Update `backend-go/database/database.go` so `AutoMigrate` includes `&BillingRecord{}`. Do not add `User`, `Task`, `Image`, `constraint`, `OnDelete`, or association fields to `BillingRecord`, because D-14 requires historical billing to survive task/user deletion.</action>
  <verify>
    <automated>cd backend-go && go test ./database/... ./service/...</automated>
  </verify>
  <acceptance_criteria>
    - `backend-go/database/models.go` contains `type BillingRecord struct`.
    - `backend-go/database/models.go` contains `EndpointBaseURLSnapshot string` and `UserLabelSnapshot string`.
    - `backend-go/database/models.go` contains `UnitCostX10000 int64`, `UnitSaleX10000 int64`, `CostX10000 int64`, `RevenueX10000 int64`, and `ProfitX10000 int64`.
    - `backend-go/database/models.go` contains `func (BillingRecord) TableName() string { return "billing_records" }`.
    - `backend-go/database/database.go` includes `&BillingRecord{}` in `AutoMigrate`.
    - `backend-go/database/models.go` does not contain `OnDelete` on the `BillingRecord` model.
  </acceptance_criteria>
  <done>`billing_records` exists as an independent historical table and is migrated during backend startup.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add billing write service for saved images</name>
  <files>backend-go/service/billing.go, backend-go/service/billing_test.go</files>
  <read_first>
    - .planning/phases/05-cost-revenue-analytics/05-CONTEXT.md
    - .planning/phases/05-cost-revenue-analytics/05-RESEARCH.md
    - backend-go/service/money.go
    - backend-go/database/models.go
    - backend-go/service/image_test.go
    - backend-go/util/id.go
  </read_first>
  <behavior>
    - `RecordBillingForSuccessfulImages` with three successful images creates exactly three `billing_records` rows.
    - Each row has a non-empty `ID` generated with `util.GenerateID()`, and multiple rows created in one call have unique IDs.
    - Each row has `success_image_count = 1`, stores the input task/user/user-label/endpoint/output-image snapshots, and computes `cost_x10000`, `revenue_x10000`, and `profit_x10000` from unit cost and sale snapshots.
    - Calling `RecordBillingForSuccessfulImages` with an empty image slice returns nil and creates zero rows.
    - Billing rows remain after deleting matching `users` and `tasks` rows in the same test database.
  </behavior>
  <action>Create `backend-go/service/billing.go` with `type BillingImageInput struct { OutputImageID string; EndpointBaseURLSnapshot string; UnitCostX10000 int64 }`, `type BillingBatchInput struct { TaskID string; UserID string; UserLabelSnapshot string; UnitSaleX10000 int64; Images []BillingImageInput; CreatedAt int64 }`, and `func RecordBillingForSuccessfulImages(input BillingBatchInput) error`. The function must import `backend-go/util` using the module's existing import path and create one `database.BillingRecord` per saved output image event. For each row, explicitly set `ID: util.GenerateID()` inside the per-image loop, not once per batch, so every billing row has a non-empty unique ID. Also set `SuccessImageCount` to `1`, set `CostX10000` to `UnitCostX10000`, set `RevenueX10000` to `UnitSaleX10000`, set `ProfitX10000` to `UnitSaleX10000 - UnitCostX10000`, use `time.Now().UnixMilli()` when `CreatedAt` is `0`, and return nil without inserts when `Images` is empty. Create `backend-go/service/billing_test.go` with temporary SQLite setup covering the behavior cases and D-12 through D-14, including an assertion that three saved images create three rows with non-empty pairwise-unique IDs.</action>
  <verify>
    <automated>cd backend-go && go test ./service/... -run 'TestRecordBillingForSuccessfulImages'</automated>
  </verify>
  <acceptance_criteria>
    - `backend-go/service/billing.go` contains `type BillingImageInput struct`.
    - `backend-go/service/billing.go` contains `type BillingBatchInput struct`.
    - `backend-go/service/billing.go` contains `func RecordBillingForSuccessfulImages(input BillingBatchInput) error`.
    - `backend-go/service/billing.go` contains `util.GenerateID()` assigned for each `database.BillingRecord` row.
    - `backend-go/service/billing_test.go` asserts three saved images create three `billing_records` rows.
    - `backend-go/service/billing_test.go` asserts multiple billing rows have non-empty unique IDs.
    - `backend-go/service/billing_test.go` asserts rows remain after deleting matching user/task records.
    - `cd backend-go && go test ./service/... -run 'TestRecordBillingForSuccessfulImages'` exits 0.
  </acceptance_criteria>
  <done>Billing persistence can write immutable per-success-image records with unique billing row IDs and without relying on task or user row lifetime.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| service billing input -> database | Billing snapshots and money values are persisted for financial reporting. |
| database migration -> existing SQLite data | AutoMigrate changes schema without destructive migration. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-01 | Tampering | `RecordBillingForSuccessfulImages` | mitigate | Compute cost/revenue/profit in service from fixed-point integer inputs; tests assert no float parsing and exact X10000 values. |
| T-05-01-02 | Information Disclosure | `billing_records` table | mitigate | Store endpoint URL and user label snapshots only; do not store endpoint API keys in billing rows. |
| T-05-01-03 | Denial of Service | AutoMigrate on startup | accept | Adds one indexed table using existing GORM AutoMigrate pattern; no external calls or unbounded migration loop. |
| T-05-01-SC | Tampering | package installs | accept | No npm, pip, or cargo package installs in this plan. |
</threat_model>

<verification>
Run `cd backend-go && go test ./service/... ./database/...` after all tasks. Confirm `backend-go/service/money.go` contains no floating-point parsing, `backend-go/database/models.go` contains no billing foreign-key cascade, and `backend-go/service/billing_test.go` asserts billing row IDs are non-empty and unique.
</verification>

<success_criteria>
- Money helper parses and formats nonnegative decimal yuan values with exactly X10000 fixed-point storage.
- `billing_records` is migrated and contains all D-13 snapshot fields.
- `RecordBillingForSuccessfulImages` writes one immutable billing row with a unique non-empty ID per saved output image and preserves history after user/task deletion.
</success_criteria>

<output>
Create `.planning/phases/05-cost-revenue-analytics/05-01-SUMMARY.md` when done.
</output>
