---
phase: 05-cost-revenue-analytics
plan: 05
type: execute
wave: 4
depends_on:
  - "05-02"
  - "05-04"
files_modified:
  - src/admin/adminApi.ts
autonomous: true
requirements:
  - COST-01
  - COST-02
  - COST-04
must_haves:
  truths:
    - "Admin frontend has typed clients for pricing config"
    - "Admin frontend has typed clients for summary, trend, endpoint breakdown, and user breakdown analytics"
    - "Money values remain X10000 integers on the API boundary"
  artifacts:
    - path: "src/admin/adminApi.ts"
      provides: "pricing and analytics TypeScript DTOs plus admin API functions"
      exports: ["adminGetPricingConfig", "adminUpdatePricingConfig", "adminGetBillingSummary", "adminGetBillingTrend", "adminGetBillingEndpointBreakdown", "adminGetBillingUserBreakdown"]
  key_links:
    - from: "src/admin/adminApi.ts"
      to: "/api/admin/config/pricing"
      via: "admin pricing client functions"
      pattern: '/api/admin/config/pricing'
    - from: "src/admin/adminApi.ts"
      to: "/api/admin/analytics/*"
      via: "admin analytics client functions"
      pattern: '/api/admin/analytics/(summary|trend|endpoints|users)'
---

<objective>
为 Phase 5 管理后台 UI 增加类型安全的 admin API 客户端：定价配置读写、四个统计数据接口和固定点金额 DTO。

Purpose: 让后续 UI 任务无需猜测后端响应形状，确保 COST-01、COST-02、COST-04 的前端边界与后端计划一致。
Output: `src/admin/adminApi.ts` 中的 pricing/analytics types 和 client functions。
</objective>

<execution_context>
@C:/Users/32478/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/32478/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-cost-revenue-analytics/05-CONTEXT.md
@.planning/phases/05-cost-revenue-analytics/05-RESEARCH.md
@.planning/phases/05-cost-revenue-analytics/05-UI-SPEC.md
@src/admin/adminApi.ts
@backend-go/handler/admin.go

<interfaces>
Existing frontend admin API pattern:
- `adminRequest<T>(path, options)` automatically prefixes `API_BASE_URL`, attaches the admin bearer token, sets JSON content type, and throws `Error` from backend `error`/`message` fields.
- `ApiEndpoint` currently has `baseUrl`, `apiKey`, `maxConcurrency?`, and `priority?`; Phase 05 adds `costPerImageX10000?: number`.
- Backend planned endpoints: `GET/PUT /api/admin/config/pricing`, `GET /api/admin/analytics/summary`, `GET /api/admin/analytics/trend`, `GET /api/admin/analytics/endpoints`, `GET /api/admin/analytics/users`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add pricing DTOs and client functions</name>
  <files>src/admin/adminApi.ts</files>
  <read_first>
    - .planning/phases/05-cost-revenue-analytics/05-CONTEXT.md
    - .planning/phases/05-cost-revenue-analytics/05-RESEARCH.md
    - .planning/phases/05-cost-revenue-analytics/05-UI-SPEC.md
    - src/admin/adminApi.ts
  </read_first>
  <behavior>
    - `ApiEndpoint` exposes optional `costPerImageX10000?: number`.
    - `adminGetPricingConfig()` calls `/api/admin/config/pricing` and returns endpoints, `salePriceX10000`, and `moneyScale`.
    - `adminUpdatePricingConfig(endpoints, salePriceX10000)` sends one PUT body containing both `endpoints` and `salePriceX10000`.
    - Existing `adminGetEndpoints` and `adminUpdateEndpoints` remain available for existing callers.
  </behavior>
  <action>Update `src/admin/adminApi.ts`: add `costPerImageX10000?: number` to `ApiEndpoint`; add `export interface PricingConfigResponse { endpoints: ApiEndpoint[]; salePriceX10000: number; moneyScale: number; ok?: true }`; add `export function adminGetPricingConfig(): Promise<PricingConfigResponse>` using `adminRequest('/api/admin/config/pricing')`; add `export function adminUpdatePricingConfig(endpoints: ApiEndpoint[], salePriceX10000: number): Promise<PricingConfigResponse>` using PUT with JSON body `{ endpoints, salePriceX10000 }`. Keep `adminGetEndpoints` and `adminUpdateEndpoints` unchanged except for the extended `ApiEndpoint` type.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/admin/adminApi.ts` contains `costPerImageX10000?: number`.
    - `src/admin/adminApi.ts` contains `export interface PricingConfigResponse`.
    - `src/admin/adminApi.ts` contains `export function adminGetPricingConfig()`.
    - `src/admin/adminApi.ts` contains `export function adminUpdatePricingConfig(endpoints: ApiEndpoint[], salePriceX10000: number)`.
    - `src/admin/adminApi.ts` contains `/api/admin/config/pricing` exactly twice.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>Frontend admin code can read and atomically save endpoint costs plus global sale price through the planned backend API.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add analytics DTOs and client functions</name>
  <files>src/admin/adminApi.ts</files>
  <read_first>
    - .planning/phases/05-cost-revenue-analytics/05-CONTEXT.md
    - .planning/phases/05-cost-revenue-analytics/05-RESEARCH.md
    - .planning/phases/05-cost-revenue-analytics/05-UI-SPEC.md
    - src/admin/adminApi.ts
  </read_first>
  <behavior>
    - Analytics range type allows only `today`, `7d`, `30d`, and `all`.
    - Summary client returns `summary` with `revenueX10000`, `costX10000`, `profitX10000`, and `successImages`.
    - Trend client returns `trend` points with `bucket`, money fields, and `successImages`.
    - Endpoint and user breakdown clients return rows with label/id fields, money fields, `successImages`, and `profitRateBps`.
  </behavior>
  <action>Update `src/admin/adminApi.ts`: add `export type AnalyticsRange = 'today' | '7d' | '30d' | 'all'`; add `AnalyticsMeta`, `BillingSummary`, `BillingTrendPoint`, `BillingEndpointRow`, and `BillingUserRow` interfaces using X10000 integer field names exactly: `revenueX10000`, `costX10000`, `profitX10000`, plus `successImages`. Add functions `adminGetBillingSummary(range: AnalyticsRange)`, `adminGetBillingTrend(range: AnalyticsRange)`, `adminGetBillingEndpointBreakdown(range: AnalyticsRange)`, and `adminGetBillingUserBreakdown(range: AnalyticsRange)`. Each function must call the corresponding backend path with `?range=${encodeURIComponent(range)}` and must not aggregate financial totals in the browser client.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/admin/adminApi.ts` contains `export type AnalyticsRange = 'today' | '7d' | '30d' | 'all'`.
    - `src/admin/adminApi.ts` contains `export interface BillingSummary` with all four summary fields.
    - `src/admin/adminApi.ts` contains `export function adminGetBillingSummary(range: AnalyticsRange)`.
    - `src/admin/adminApi.ts` contains `export function adminGetBillingTrend(range: AnalyticsRange)`.
    - `src/admin/adminApi.ts` contains `export function adminGetBillingEndpointBreakdown(range: AnalyticsRange)`.
    - `src/admin/adminApi.ts` contains `export function adminGetBillingUserBreakdown(range: AnalyticsRange)`.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>Frontend has a typed API boundary for all analytics blocks without introducing client-side finance aggregation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin UI -> admin API | Browser code sends authenticated requests with admin token. |
| API DTO -> UI components | Financial fields cross into React rendering as fixed-point integers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05-01 | Tampering | `adminUpdatePricingConfig` body | mitigate | Function sends both endpoint costs and sale price in a single JSON body to backend validation; no partial row save client function. |
| T-05-05-02 | Information Disclosure | admin API client | mitigate | Uses existing admin token boundary; no public user-facing pricing/statistics functions added. |
| T-05-05-SC | Tampering | package installs | accept | No npm, pip, or cargo package installs in this plan. |
</threat_model>

<verification>
Run `npm run build`. Source-check that all money DTO fields use `X10000` names and no new chart or data-fetching dependencies were added.
</verification>

<success_criteria>
- `src/admin/adminApi.ts` exports typed pricing and analytics functions for all planned UI features.
- Client functions keep backend fixed-point money values intact and do not add export, drill-down, or user-visible pricing APIs.
</success_criteria>

<output>
Create `.planning/phases/05-cost-revenue-analytics/05-05-SUMMARY.md` when done.
</output>
