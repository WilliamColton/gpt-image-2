---
phase: 05-cost-revenue-analytics
plan: 07
type: execute
wave: 6
depends_on:
  - "05-06"
files_modified:
  - src/admin/AdminDashboard.tsx
autonomous: true
requirements:
  - COST-04
must_haves:
  truths:
    - "管理后台新增成本收益统计 tab，位置在系统配置之后、公告管理之前"
    - "统计页默认时间范围是 7天"
    - "统计页展示总收入、总成本、利润、成功图片数四个 KPI"
    - "统计页展示收入/成本/利润/成功图片数趋势"
    - "统计页展示端点拆分和用户拆分表格"
    - "时间筛选只支持今日、7天、30天、全部"
  artifacts:
    - path: "src/admin/AdminDashboard.tsx"
      provides: "admin cost/revenue analytics tab with KPI, trend, endpoint/user breakdowns"
      contains: "成本收益统计"
  key_links:
    - from: "src/admin/AdminDashboard.tsx"
      to: "src/admin/adminApi.ts"
      via: "uses adminGetBillingSummary/trend/breakdown functions"
      pattern: 'adminGetBilling(Summary|Trend|EndpointBreakdown|UserBreakdown)'
---

<objective>
在现有 `/admin` tabs 壳层中新增“成本收益统计”页面，展示 KPI、时间筛选、自定义轻量趋势图、端点拆分和用户拆分表格。

Purpose: 实现 COST-04 以及 D-08、D-09、D-10、D-11，并遵守 UI-SPEC：不引入 chart 依赖、不新增独立路由、不提供导出或钻取。
Output: `AdminDashboard.tsx` 中的 analytics tab、独立加载状态、空/错状态、轻量 SVG 趋势图、拆分表。
</objective>

<execution_context>
@C:/Users/32478/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/32478/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-cost-revenue-analytics/05-CONTEXT.md
@.planning/phases/05-cost-revenue-analytics/05-RESEARCH.md
@.planning/phases/05-cost-revenue-analytics/05-UI-SPEC.md
@src/admin/AdminDashboard.tsx
@src/admin/adminApi.ts
@src/components/ui/card.tsx
@src/components/ui/table.tsx
@src/components/ui/tabs.tsx
@src/components/ui/badge.tsx
@src/components/ui/empty-state.tsx
@package.json

<interfaces>
Frontend API from Plan 05-05:
- `AnalyticsRange = 'today' | '7d' | '30d' | 'all'`.
- `adminGetBillingSummary(range)` returns `summary` with `revenueX10000`, `costX10000`, `profitX10000`, and `successImages`.
- `adminGetBillingTrend(range)` returns `trend` points with `bucket`, money fields, and `successImages`.
- `adminGetBillingEndpointBreakdown(range)` and `adminGetBillingUserBreakdown(range)` return `rows` sorted by backend.
UI contract exact copy:
- Tab label: `成本收益统计`, placed after `系统配置` and before `公告管理`.
- Default range: `7天`.
- Range labels: `今日`, `7天`, `30天`, `全部`.
- KPI labels: `总收入`, `总成本`, `利润`, `成功图片数`.
- Empty heading: `暂无成本收益数据`.
- Empty body: `完成图片生成并保存价格配置后，这里将显示收入、成本、利润和成功图片数。`.
- Error text: `统计数据加载失败，请点击“刷新统计”重试；保存价格失败时，请检查金额是否为数字且最多 4 位小数。`.
- Refresh success toast: `统计已刷新`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add analytics tab state, KPI cards, and lightweight trend chart</name>
  <files>src/admin/AdminDashboard.tsx</files>
  <read_first>
    - .planning/phases/05-cost-revenue-analytics/05-UI-SPEC.md
    - src/admin/AdminDashboard.tsx
    - src/admin/adminApi.ts
    - src/components/ui/card.tsx
    - src/components/ui/empty-state.tsx
  </read_first>
  <behavior>
    - The `Tab` union includes `analytics` and the tabs list renders `成本收益统计` after `系统配置` and before `公告管理`.
    - Analytics range state defaults to `7d` and renders as `7天` in the selected filter.
    - Loading analytics calls summary and trend functions independently so one failure does not clear the other block.
    - KPI row displays `总收入`, `总成本`, `利润`, and `成功图片数` using tabular numerals.
    - Trend card visualizes income/cost/profit as colored series and success image count as a second lower series on the same time axis.
    - No chart dependency is added to `package.json`; chart is implemented with inline SVG or equivalent local React markup.
    - Empty data keeps cards visible and shows `暂无成本收益数据` with the specified body copy.
  </behavior>
  <action>Update `src/admin/AdminDashboard.tsx`: import analytics API functions and types from `./adminApi`; extend `type Tab` with `analytics`; add `<TabsTrigger value="analytics">成本收益统计</TabsTrigger>` immediately after the `config` trigger and before `announcement`. Add state for `analyticsRange` defaulting to `'7d'`, plus separate state buckets for summary, trend, loading flags, and error strings. Add `loadAnalyticsSummary` and `loadAnalyticsTrend` helpers that call `adminGetBillingSummary` and `adminGetBillingTrend` independently. In the existing tab-loading `useEffect`, call analytics loading only when `tab === 'analytics'`. Update the header refresh button so when `tab === 'analytics'` it reloads analytics and shows toast `统计已刷新` after successful refresh of at least one block. Add formatting helpers `formatMoneyX10000(value: number, moneyScale?: number)` and `formatProfitClass(value: number)` for right-aligned, tabular money display with up to 4 decimals. Render a responsive KPI grid with four cards labelled `总收入`, `总成本`, `利润`, `成功图片数`. Render a trend card with a header containing range filter buttons `今日`, `7天`, `30天`, `全部` and a `刷新统计` button. Implement a lightweight chart using inline SVG paths/lines/rects in `AdminDashboard.tsx`: income color blue, cost amber, profit emerald or red when negative, success image count violet or zinc; include always-visible legend and `title` elements or accessible labels with exact bucket/value for hover. Show skeleton/loading blocks during loading. If all summary money/count values are zero and trend is empty, keep the cards and render `EmptyState` with title `暂无成本收益数据` and description `完成图片生成并保存价格配置后，这里将显示收入、成本、利润和成功图片数。`. Do not install or import `recharts`, `chart.js`, `echarts`, or any new chart package.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/admin/AdminDashboard.tsx` contains `type Tab =` with `analytics`.
    - `src/admin/AdminDashboard.tsx` contains `<TabsTrigger value="analytics">成本收益统计</TabsTrigger>` after the `config` trigger and before the `announcement` trigger.
    - `src/admin/AdminDashboard.tsx` contains `useState<AnalyticsRange>('7d')` or equivalent default range state.
    - `src/admin/AdminDashboard.tsx` imports analytics API client functions.
    - `src/admin/AdminDashboard.tsx` contains labels `总收入`, `总成本`, `利润`, and `成功图片数`.
    - `src/admin/AdminDashboard.tsx` contains labels `今日`, `7天`, `30天`, and `全部`.
    - `src/admin/AdminDashboard.tsx` contains `刷新统计`.
    - `src/admin/AdminDashboard.tsx` contains `<svg` for the custom chart.
    - `src/admin/AdminDashboard.tsx` contains `暂无成本收益数据` and the exact empty-state body copy.
    - `package.json` does not contain `recharts`, `chart.js`, or `echarts`.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>Analytics page shows KPI-first summary and lightweight trend visualization without new chart dependencies.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render endpoint and user breakdown tables with error states</name>
  <files>src/admin/AdminDashboard.tsx</files>
  <read_first>
    - .planning/phases/05-cost-revenue-analytics/05-UI-SPEC.md
    - src/admin/AdminDashboard.tsx
    - src/admin/adminApi.ts
    - src/components/ui/table.tsx
    - src/components/ui/card.tsx
  </read_first>
  <behavior>
    - Bottom analytics area renders two cards: `端点拆分` and `用户拆分`.
    - Endpoint table columns are endpoint identifier, success image count, revenue, cost, profit, and profit rate.
    - User table columns are user identifier, success image count, revenue, cost, profit, and profit rate.
    - Money columns are right aligned with tabular numerals; negative profit is red, zero neutral, positive green.
    - Failed blocks show the specified error text and a single `刷新统计` retry action without clearing successfully loaded blocks.
    - There is no CSV/PDF export, no row click drill-down, no endpoint/user filters, and no search.
  </behavior>
  <action>Render the analytics tab bottom area with two responsive cards titled `端点拆分` and `用户拆分`, side-by-side on desktop and stacked on mobile. Use existing `Table`, `TableHeader`, `TableBody`, `TableRow`, `TableHead`, and `TableCell`. Endpoint columns must be `端点标识`, `成功图片数`, `收入`, `成本`, `利润`, `利润率`; user columns must be `用户标识`, `成功图片数`, `收入`, `成本`, `利润`, `利润率`. Trust backend sorting and do not add client sorting controls. Use `profitRateBps` formatted as percent with up to 2 decimals. For errors, show the exact message `统计数据加载失败，请点击“刷新统计”重试；保存价格失败时，请检查金额是否为数字且最多 4 位小数。` inside the affected card and provide one `刷新统计` button that reloads analytics. Do not add `onClick` handlers to table rows, export buttons, search boxes, endpoint/user filters, CSV/PDF labels, or drill-down detail UI.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/admin/AdminDashboard.tsx` contains `端点拆分` and `用户拆分`.
    - `src/admin/AdminDashboard.tsx` contains `端点标识` and `用户标识`.
    - `src/admin/AdminDashboard.tsx` contains `利润率`.
    - `src/admin/AdminDashboard.tsx` contains exact error text `统计数据加载失败，请点击“刷新统计”重试；保存价格失败时，请检查金额是否为数字且最多 4 位小数。`.
    - `src/admin/AdminDashboard.tsx` does not contain `CSV`, `PDF`, `导出`, or `钻取` in the analytics tab implementation.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>Analytics tab completes required endpoint/user breakdowns, partial failure behavior, and out-of-scope interaction exclusions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin analytics API -> React UI | Financial aggregates render in browser as admin-only data. |
| admin input -> range query | Time filter controls backend range query values. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07-01 | Information Disclosure | analytics tab | mitigate | Render analytics only inside existing authenticated admin dashboard; do not add public routes or user-facing price UI. |
| T-05-07-02 | Tampering | range filter | mitigate | Use `AnalyticsRange` union with only `today`, `7d`, `30d`, `all`; backend rejects invalid range. |
| T-05-07-03 | Denial of Service | refresh button | accept | Refresh triggers bounded admin API requests and reuses existing admin auth; no export or bulk download added. |
| T-05-07-SC | Tampering | package installs | mitigate | No chart package installs; source-check package.json for no `recharts`, `chart.js`, or `echarts`. |
</threat_model>

<verification>
Run `npm run build`. Source-check tab order, default range, KPI labels, chart markup, table labels, and absence of export/drill-down interactions using the acceptance criteria above.
</verification>

<success_criteria>
- Admin analytics tab fulfills UI-SPEC layout and copy requirements.
- It loads all four required analytics dimensions and handles partial failures without clearing successful blocks.
- It uses existing UI primitives and custom lightweight chart markup with no new chart dependency.
</success_criteria>

<output>
Create `.planning/phases/05-cost-revenue-analytics/05-07-SUMMARY.md` when done.
</output>
