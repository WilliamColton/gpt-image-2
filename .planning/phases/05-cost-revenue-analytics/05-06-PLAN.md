---
phase: 05-cost-revenue-analytics
plan: 06
type: execute
wave: 5
depends_on:
  - "05-05"
files_modified:
  - src/admin/AdminDashboard.tsx
autonomous: true
requirements:
  - COST-01
  - COST-02
must_haves:
  truths:
    - "系统配置页为每个 API 端点显示成本价（元/张）输入"
    - "系统配置页显示全局售价（元/张）输入"
    - "保存价格配置一次性提交端点成本价和全局售价"
    - "价格输入为空、负数或超过 4 位小数时不能保存并显示内联错误"
  artifacts:
    - path: "src/admin/AdminDashboard.tsx"
      provides: "admin pricing configuration UI inside existing config tab"
      contains: "保存价格配置"
  key_links:
    - from: "src/admin/AdminDashboard.tsx"
      to: "src/admin/adminApi.ts"
      via: "uses adminGetPricingConfig and adminUpdatePricingConfig"
      pattern: 'admin(Get|Update)PricingConfig'
---

<objective>
把端点成本价和全局售价配置加入现有管理后台“系统配置”tab，使用固定点金额转换和一次性保存价格配置。

Purpose: 实现 COST-01、COST-02 以及 D-04、D-05、D-07，遵守 UI-SPEC 的定价配置合同：端点成本价、全局售价、最多 4 位小数、单一 CTA。
Output: `AdminDashboard.tsx` 系统配置页中的成本价输入、全局售价卡片、校验、保存价格配置按钮和 toast。
</objective>

<execution_context>
@C:/Users/32478/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/32478/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-cost-revenue-analytics/05-CONTEXT.md
@.planning/phases/05-cost-revenue-analytics/05-RESEARCH.md
@.planning/phases/05-cost-revenue-analytics/05-UI-SPEC.md
@src/admin/AdminDashboard.tsx
@src/admin/adminApi.ts
@src/components/ui/card.tsx
@src/components/ui/input.tsx
@src/components/ui/button.tsx
@src/components/ui/label.tsx

<interfaces>
Frontend API from Plan 05-05:
- `adminGetPricingConfig(): Promise<{ endpoints: ApiEndpoint[]; salePriceX10000: number; moneyScale: number }>`.
- `adminUpdatePricingConfig(endpoints: ApiEndpoint[], salePriceX10000: number)` saves endpoint costs and global sale price in one request.
- `ApiEndpoint.costPerImageX10000?: number` is the fixed-point endpoint cost value.
UI contract exact copy:
- Endpoint input label: `成本价（元/张）`.
- Global card label: `全局售价（元/张）`.
- Primary CTA text: `保存价格配置`.
- Success toast: `价格配置已保存`.
- Help text: `支持 4 位小数`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Load pricing config and add money input state</name>
  <files>src/admin/AdminDashboard.tsx</files>
  <read_first>
    - .planning/phases/05-cost-revenue-analytics/05-UI-SPEC.md
    - src/admin/AdminDashboard.tsx
    - src/admin/adminApi.ts
  </read_first>
  <behavior>
    - Switching to `config` tab loads pricing through `adminGetPricingConfig` instead of only `adminGetEndpoints`.
    - Endpoint cost inputs display yuan strings converted from `costPerImageX10000` with up to 4 decimal places.
    - Global sale price input displays a yuan string converted from `salePriceX10000` with up to 4 decimal places.
  </behavior>
  <action>Update `src/admin/AdminDashboard.tsx` imports to include `adminGetPricingConfig`, `adminUpdatePricingConfig`, and type `PricingConfigResponse` if needed. Add local state for `salePriceInput`, `priceErrors`, and `pricingSaving`. Replace the `loadEndpoints` implementation for the config tab with a pricing load that calls `adminGetPricingConfig()`, sets `endpoints`, converts each `costPerImageX10000 ?? 0` to a string with no trailing zeros beyond 4 decimals, and sets `salePriceInput` from `salePriceX10000`. Add local helper functions `formatMoneyInputFromX10000(value: number): string` and `parseMoneyInputToX10000(value: string): number | null` inside the component or module; parsing must use string/integer arithmetic and reject empty, negative, non-numeric, and more than 4 decimal places. Do not use `parseFloat` for validation because D-07 requires avoiding floating-point money errors.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/admin/AdminDashboard.tsx` imports `adminGetPricingConfig` and `adminUpdatePricingConfig`.
    - `src/admin/AdminDashboard.tsx` contains `salePriceInput` state.
    - `src/admin/AdminDashboard.tsx` contains `formatMoneyInputFromX10000`.
    - `src/admin/AdminDashboard.tsx` contains `parseMoneyInputToX10000`.
    - `src/admin/AdminDashboard.tsx` does not contain `parseFloat`.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>System config tab can load and represent fixed-point price values as precise yuan input strings.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render endpoint cost and global sale price controls</name>
  <files>src/admin/AdminDashboard.tsx</files>
  <read_first>
    - .planning/phases/05-cost-revenue-analytics/05-UI-SPEC.md
    - src/admin/AdminDashboard.tsx
    - src/components/ui/card.tsx
    - src/components/ui/input.tsx
    - src/components/ui/label.tsx
  </read_first>
  <behavior>
    - Every endpoint card includes a `成本价（元/张）` input beside existing Base URL, API Key, maximum concurrency, and priority controls.
    - A separate global sale price card appears below the endpoint pool card with label `全局售价（元/张）` and help text `支持 4 位小数`.
    - Price inputs use `type="number"`, `min="0"`, `step="0.0001"`, and right-aligned tabular numeric styling.
    - Invalid price fields show inline error text below the field.
  </behavior>
  <action>Update the `tab === 'config'` render block in `src/admin/AdminDashboard.tsx`. Add each endpoint's `成本价（元/张）` input to the existing endpoint form and route changes through `handleEndpointChange(i, 'costPerImageX10000', parsedOrDraftValue)` or a dedicated `handleEndpointCostInputChange` that keeps a precise string draft until save. Add a separate `Card` or matching rounded panel below the endpoint pool titled `全局售价（元/张）`, with one right-aligned numeric input using `step="0.0001"`, `min="0"`, and helper text `支持 4 位小数`. Inline errors must use concrete messages: `请输入非负数字，最多 4 位小数` for invalid price input. Keep the existing admin shell, width, header, theme selector, logout, and refresh interactions unchanged. Do not add export, drill-down, per-user price, payment, or user-facing price display controls.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/admin/AdminDashboard.tsx` contains `成本价（元/张）`.
    - `src/admin/AdminDashboard.tsx` contains `全局售价（元/张）`.
    - `src/admin/AdminDashboard.tsx` contains `step="0.0001"`.
    - `src/admin/AdminDashboard.tsx` contains `支持 4 位小数`.
    - `src/admin/AdminDashboard.tsx` contains `请输入非负数字，最多 4 位小数`.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>Admins can edit every endpoint cost and the global sale price in the existing system configuration UI.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Save all price fields atomically</name>
  <files>src/admin/AdminDashboard.tsx</files>
  <read_first>
    - .planning/phases/05-cost-revenue-analytics/05-CONTEXT.md
    - .planning/phases/05-cost-revenue-analytics/05-UI-SPEC.md
    - src/admin/AdminDashboard.tsx
    - src/admin/adminApi.ts
  </read_first>
  <behavior>
    - `保存价格配置` is disabled when any endpoint cost or global sale price is empty, negative, non-numeric, or more than 4 decimal places.
    - Clicking `保存价格配置` sends one `adminUpdatePricingConfig(validEndpoints, salePriceX10000)` request containing all endpoint `costPerImageX10000` values and the global sale price.
    - On success, the UI reloads pricing config, keeps the user on the config tab, and shows toast `价格配置已保存`.
    - The legacy endpoint config save button is either replaced by the single pricing CTA or no longer produces a separate partial price save path.
  </behavior>
  <action>Replace the config tab save behavior with `handleSavePricingConfig`. It must validate all endpoint base URLs as before, validate all endpoint cost inputs and `salePriceInput` using `parseMoneyInputToX10000`, build endpoints with `costPerImageX10000` integer values, and call `adminUpdatePricingConfig(validEndpoints, salePriceX10000)` once. Use button text exactly `保存价格配置`; show loading text `保存中...`; show success toast exactly `价格配置已保存`; on failure show the backend/client error via existing `toast(..., 'error')`. Keep scroll position by not navigating or remounting the admin page. Do not create per-row save buttons because UI-SPEC requires a single CTA to avoid inconsistent snapshots.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/admin/AdminDashboard.tsx` contains `handleSavePricingConfig`.
    - `src/admin/AdminDashboard.tsx` contains `adminUpdatePricingConfig(valid` or otherwise calls `adminUpdatePricingConfig` once in the save handler.
    - `src/admin/AdminDashboard.tsx` contains button text `保存价格配置`.
    - `src/admin/AdminDashboard.tsx` contains toast text `价格配置已保存`.
    - `src/admin/AdminDashboard.tsx` does not contain a config save button text `保存配置` in the pricing config tab.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>Pricing configuration saves endpoint costs and global sale price as one validated operation with clear admin feedback.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin browser input -> pricing API | Admin-entered money strings become financial configuration values. |
| pricing API response -> admin UI | API keys and price fields render in admin-only interface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06-01 | Tampering | price input parsing | mitigate | Reject empty, negative, non-numeric, and >4-decimal values before enabling save; backend also validates fixed-point integer values. |
| T-05-06-02 | Information Disclosure | endpoint config UI | mitigate | Keep all pricing controls inside existing authenticated `/admin` shell and do not add user-facing price display. |
| T-05-06-03 | Repudiation | partial pricing saves | mitigate | Use one `保存价格配置` CTA calling one API request for all endpoint costs plus sale price. |
| T-05-06-SC | Tampering | package installs | accept | No npm, pip, or cargo package installs in this plan. |
</threat_model>

<verification>
Run `npm run build`. Source-check that `AdminDashboard.tsx` contains the exact copy `成本价（元/张）`, `全局售价（元/张）`, `保存价格配置`, and does not use `parseFloat` for money parsing.
</verification>

<success_criteria>
- Admin system config UI edits endpoint costs and global sale price with precise 4-decimal validation.
- Save operation is atomic from the UI perspective and uses the Phase 5 pricing API.
- No out-of-scope export, drill-down, per-user pricing, payment, or user-facing pricing controls are introduced.
</success_criteria>

<output>
Create `.planning/phases/05-cost-revenue-analytics/05-06-SUMMARY.md` when done.
</output>
