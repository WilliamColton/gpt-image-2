---
phase: 05-cost-revenue-analytics
plan: 03
type: execute
wave: 3
depends_on:
  - "05-01"
  - "05-02"
files_modified:
  - backend-go/service/openai.go
  - backend-go/service/openai_failover_test.go
  - backend-go/handler/generate.go
  - backend-go/handler/generate_billing_test.go
autonomous: true
requirements:
  - COST-03
must_haves:
  truths:
    - "成功生成任务只按实际保存进 outputImages 的图片数记账"
    - "部分保存成功时只为保存成功图片写账单"
    - "failover 失败端点不产生成本，成本归最终成功端点"
    - "每条账单保存生成时的端点成本价和全局售价快照"
  artifacts:
    - path: "backend-go/service/openai.go"
      provides: "endpoint attribution on generated images"
      contains: "EndpointBaseURL"
    - path: "backend-go/handler/generate.go"
      provides: "billing integration after successful image save"
      contains: "RecordBillingForSuccessfulImages"
    - path: "backend-go/handler/generate_billing_test.go"
      provides: "tests for outputImages-based billing and failover endpoint attribution"
      contains: "TestExecuteImageGenerationRecordsBillingForSavedImages"
  key_links:
    - from: "backend-go/service/openai.go"
      to: "backend-go/handler/generate.go"
      via: "GeneratedImage.EndpointBaseURL travels with each generated image"
      pattern: 'EndpointBaseURL'
    - from: "backend-go/handler/generate.go"
      to: "backend-go/service/billing.go"
      via: "RecordBillingForSuccessfulImages called after saved image IDs are known"
      pattern: 'RecordBillingForSuccessfulImages'
---

<objective>
把记账接入真实生成成功路径：OpenAI/failover 返回成功端点归属，生成 handler 保存图片后按保存成功事件写账单，并修复保存成功 metadata 与 result 图片错位问题。

Purpose: 实现 D-01、D-02、D-03、D-06、D-13 的记账口径，确保账单基于 `outputImages` 而不是请求数量、API 返回数量或失败端点。
Output: endpoint attribution fields、保存成功 slice、生成完成后 billing 写入、相关测试。
</objective>

<execution_context>
@C:/Users/32478/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/32478/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-cost-revenue-analytics/05-CONTEXT.md
@.planning/phases/05-cost-revenue-analytics/05-RESEARCH.md
@.planning/phases/03-api-failover/03-02-SUMMARY.md
@.planning/phases/04-admin-dashboard/04-01-SUMMARY.md
@backend-go/service/openai.go
@backend-go/handler/generate.go
@backend-go/service/billing.go
@backend-go/config/config.go
@backend-go/middleware/middleware.go

<interfaces>
Existing generation contracts:
- `service.GeneratedImage` currently includes `Base64`, `ActualParams`, and `RevisedPrompt`.
- `service.ImageGenResult` currently includes `Images []GeneratedImage` and `ActualParams`.
- `withFailover` can see `ep.BaseURL` and `ep.CostPerImageX10000` after Plan 02.
- `executeImageGeneration` saves images via `saveGeneratedImages`, increments `used_count`, builds per-image metadata, then upserts the task.
- `middleware.GetAuthUser(c)` returns `AuthUser` with `ID` and `Label`; use this label snapshot for billing.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Carry successful endpoint attribution through OpenAI results</name>
  <files>backend-go/service/openai.go, backend-go/service/openai_failover_test.go</files>
  <read_first>
    - .planning/phases/05-cost-revenue-analytics/05-CONTEXT.md
    - .planning/phases/05-cost-revenue-analytics/05-RESEARCH.md
    - backend-go/service/openai.go
    - backend-go/config/config.go
    - backend-go/service/openai_failover_test.go
  </read_first>
  <behavior>
    - When `withFailover` succeeds on an endpoint, every returned `GeneratedImage` has `EndpointBaseURL` equal to that endpoint `BaseURL`.
    - Every returned `GeneratedImage` has `UnitCostX10000` equal to that endpoint `CostPerImageX10000`.
    - Failed endpoint attempts do not stamp images and do not contribute cost.
    - Concurrent generation merge preserves each image's endpoint attribution from its own successful request.
  </behavior>
  <action>Update `backend-go/service/openai.go`: add `EndpointBaseURL string` and `UnitCostX10000 int64` to `GeneratedImage`. In `withFailover`, after `fn(apiKey, ep.BaseURL)` succeeds, loop through `result.Images` and set missing `EndpointBaseURL` to `ep.BaseURL` and `UnitCostX10000` to `ep.CostPerImageX10000`, then return the result. Do not stamp failed attempts. Ensure `mergeConcurrentResults` appends `GeneratedImage` values without clearing attribution. Update `backend-go/service/openai_failover_test.go` to cover failover attribution: first endpoint returns an error, second endpoint succeeds with `BaseURL: "https://success.example/v1"` and `CostPerImageX10000: 2345`, and the resulting image has those two values.</action>
  <verify>
    <automated>cd backend-go && go test ./service/... -run 'Test.*Failover.*Attribution|Test.*Endpoint.*Attribution|Test.*Failover'</automated>
  </verify>
  <acceptance_criteria>
    - `backend-go/service/openai.go` `GeneratedImage` contains `EndpointBaseURL string`.
    - `backend-go/service/openai.go` `GeneratedImage` contains `UnitCostX10000 int64`.
    - `backend-go/service/openai.go` stamps `ep.BaseURL` into successful result images.
    - `backend-go/service/openai.go` stamps `ep.CostPerImageX10000` into successful result images.
    - `backend-go/service/openai_failover_test.go` asserts failed first endpoint does not determine image endpoint attribution.
    - `cd backend-go && go test ./service/... -run 'Test.*Failover.*Attribution|Test.*Endpoint.*Attribution|Test.*Failover'` exits 0.
  </acceptance_criteria>
  <done>Every generated image carries the endpoint and unit cost snapshot of the endpoint that actually succeeded.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write billing after successful image save</name>
  <files>backend-go/handler/generate.go, backend-go/handler/generate_billing_test.go</files>
  <read_first>
    - .planning/phases/05-cost-revenue-analytics/05-CONTEXT.md
    - .planning/phases/05-cost-revenue-analytics/05-RESEARCH.md
    - backend-go/handler/generate.go
    - backend-go/service/openai.go
    - backend-go/service/billing.go
    - backend-go/service/image.go
    - backend-go/config/config.go
    - backend-go/middleware/middleware.go
  </read_first>
  <behavior>
    - If `result.Images` has three images but only two save successfully, task `OutputImages` has two IDs and billing creates two rows.
    - Billing rows use `task.ID`, authenticated `userID`, authenticated `user.Label`, successful image endpoint URL, successful image unit cost, and current `config.GetSalePriceX10000()` as immutable snapshots.
    - `used_count` increments by the same successful saved-image count used for billing.
    - The metadata maps use saved-image-to-generated-image pairing and do not shift when an earlier image save fails.
  </behavior>
  <action>Update `backend-go/handler/generate.go`: change `executeImageGeneration` signature to include `userLabel string` and update the goroutine call in `GenerateImage` to pass `user.Label`. Replace `saveGeneratedImages` return type with a saved-success slice, e.g. `type savedGeneratedImage struct { OutputImageID string; Generated service.GeneratedImage }`, so each saved output ID remains paired with the exact generated image that saved successfully. Use that slice to build `outputIDs`, increment `used_count`, build metadata, and call `service.RecordBillingForSuccessfulImages` after output IDs are known. The billing input must set `TaskID`, `UserID`, `UserLabelSnapshot`, `UnitSaleX10000: config.GetSalePriceX10000()`, and one `BillingImageInput` per saved success with `OutputImageID`, `EndpointBaseURLSnapshot: saved.Generated.EndpointBaseURL`, and `UnitCostX10000: saved.Generated.UnitCostX10000`. Do not write billing before image save; do not bill failed saves; do not bill based on requested `params.N`; do not query current endpoint config to infer endpoint attribution. Create `backend-go/handler/generate_billing_test.go` to test saved-count billing and metadata pairing with a controllable save path or helper-level test around the new saved-success builder.</action>
  <verify>
    <automated>cd backend-go && go test ./handler/... -run 'Test.*Billing|Test.*SavedGeneratedImages|Test.*PerImageMetadata'</automated>
  </verify>
  <acceptance_criteria>
    - `backend-go/handler/generate.go` calls `executeImageGeneration(user.ID, user.Label, task, req.Params, req.CodexCli, "")` or an equivalent signature with user label passed.
    - `backend-go/handler/generate.go` contains `RecordBillingForSuccessfulImages` after output image IDs are constructed.
    - `backend-go/handler/generate.go` does not call `RecordBillingForSuccessfulImages` before `SaveDataURLImage`.
    - `backend-go/handler/generate.go` uses `config.GetSalePriceX10000()` for sale price snapshot.
    - `backend-go/handler/generate_billing_test.go` verifies billing row count equals successful saved image count, not requested `n` or API result count.
    - `cd backend-go && go test ./handler/... -run 'Test.*Billing|Test.*SavedGeneratedImages|Test.*PerImageMetadata'` exits 0.
  </acceptance_criteria>
  <done>Generation success writes immutable billing rows exactly for saved output image events and preserves correct per-image metadata pairing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OpenAI/failover -> generation handler | External endpoint response enters backend execution flow with endpoint attribution. |
| generation handler -> billing service | Successful saved image events become financial records. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-01 | Tampering | `withFailover` endpoint attribution | mitigate | Stamp endpoint URL and unit cost only after a successful endpoint call returns; tests cover failed first endpoint and successful second endpoint. |
| T-05-03-02 | Tampering | `executeImageGeneration` billing call | mitigate | Build billing inputs from saved-success slice after `SaveDataURLImage` succeeds; tests assert row count equals saved count. |
| T-05-03-03 | Repudiation | billing snapshot source | mitigate | Store task ID, user ID, user label snapshot, endpoint URL snapshot, unit cost, unit sale, and created time in each billing row. |
| T-05-03-SC | Tampering | package installs | accept | No npm, pip, or cargo package installs in this plan. |
</threat_model>

<verification>
Run `cd backend-go && go test ./...`. Source-check that `RecordBillingForSuccessfulImages` is invoked only after image save success data exists and that no billing logic uses requested `params.N` as the billed count.
</verification>

<success_criteria>
- Failover endpoint attribution flows from the actual successful endpoint into each generated image.
- Billing row count equals actual saved `outputImages` count.
- Billing records use historical price snapshots and never charge failed endpoint attempts or failed image saves.
</success_criteria>

<output>
Create `.planning/phases/05-cost-revenue-analytics/05-03-SUMMARY.md` when done.
</output>
