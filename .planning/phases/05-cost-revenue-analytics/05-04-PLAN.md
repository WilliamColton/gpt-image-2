---
phase: 05-cost-revenue-analytics
plan: 04
type: execute
wave: 3
depends_on:
  - "05-01"
  - "05-02"
files_modified:
  - backend-go/service/analytics.go
  - backend-go/service/analytics_test.go
  - backend-go/handler/admin.go
  - backend-go/handler/admin_analytics_test.go
  - backend-go/main.go
autonomous: true
requirements:
  - COST-04
must_haves:
  truths:
    - "D-09: 管理员 API 可以按今日/7天/30天/全部查询总收入、总成本、利润、成功图片数; D-10: time filter supports today, 7 days, 30 days, all"
    - "D-08: 管理员 API 可以按同一时间范围查询趋势、端点拆分和用户拆分"
    - "统计直接聚合 billing_records 快照，不依赖当前 users/tasks/config"
    - "拆分结果按利润降序、收入次序排序"
    - "D-11: first version only exposes chart/table data; no CSV export or drill-down detail endpoints"
  artifacts:
    - path: "backend-go/service/analytics.go"
      provides: "billing analytics queries and DTOs"
      exports: ["AnalyticsRange", "GetBillingSummary", "GetBillingTrend", "GetBillingEndpointBreakdown", "GetBillingUserBreakdown"]
    - path: "backend-go/handler/admin.go"
      provides: "admin analytics HTTP handlers"
      exports: ["AdminBillingSummary", "AdminBillingTrend", "AdminBillingEndpointBreakdown", "AdminBillingUserBreakdown"]
    - path: "backend-go/main.go"
      provides: "admin analytics route registration"
      contains: "/analytics/"
  key_links:
    - from: "backend-go/handler/admin.go"
      to: "backend-go/service/analytics.go"
      via: "handlers call service analytics functions with parsed range"
      pattern: 'GetBilling(Summary|Trend|EndpointBreakdown|UserBreakdown)'
    - from: "backend-go/service/analytics.go"
      to: "backend-go/database/models.go"
      via: "queries use database.BillingRecord only"
      pattern: 'database\.BillingRecord'
---

<objective>
提供管理后台统计页需要的后端聚合 API：summary、trend、endpoint breakdown、user breakdown 四组独立接口，共用时间范围解析和固定点金额 DTO。

Purpose: 实现 D-08、D-09、D-10、D-11 的后端统计数据来源，并满足研究建议的独立加载/partial failure 结构。
Output: billing analytics service、管理员统计 handlers、路由注册、聚合测试。
</objective>

<execution_context>
@C:/Users/32478/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/32478/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-cost-revenue-analytics/05-CONTEXT.md
@.planning/phases/05-cost-revenue-analytics/05-RESEARCH.md
@backend-go/database/models.go
@backend-go/service/billing.go
@backend-go/service/money.go
@backend-go/handler/admin.go
@backend-go/main.go

<interfaces>
New billing foundation from Plan 05-01:
- `database.BillingRecord` stores `CreatedAt`, `SuccessImageCount`, `RevenueX10000`, `CostX10000`, `ProfitX10000`, `EndpointBaseURLSnapshot`, `UserID`, and `UserLabelSnapshot`.
- `service.MoneyScale` is `10000` and must be returned to clients as `meta.moneyScale` in every analytics response wrapper.
- Analytics must query billing snapshots directly; do not join current users, tasks, or endpoint config for financial values.
Shared-file dependency:
- This plan edits `backend-go/handler/admin.go` and `backend-go/main.go` after Plan 05-02, so it depends on 05-02 to avoid route/handler merge conflicts.
Backend response wrappers consumed by Plan 05-05 TypeScript interfaces:
- `GET /api/admin/analytics/summary` returns `{ meta: { range, from, to, moneyScale }, summary: { revenueX10000, costX10000, profitX10000, successImages } }`.
- `GET /api/admin/analytics/trend` returns `{ meta: { range, from, to, moneyScale }, trend: [{ bucket, revenueX10000, costX10000, profitX10000, successImages }] }`.
- `GET /api/admin/analytics/endpoints` returns `{ meta: { range, from, to, moneyScale }, rows: [{ endpointBaseUrl, endpointLabel, successImages, revenueX10000, costX10000, profitX10000, profitRateBps }] }`.
- `GET /api/admin/analytics/users` returns `{ meta: { range, from, to, moneyScale }, rows: [{ userId, userLabel, successImages, revenueX10000, costX10000, profitX10000, profitRateBps }] }`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement billing analytics service</name>
  <files>backend-go/service/analytics.go, backend-go/service/analytics_test.go</files>
  <read_first>
    - .planning/phases/05-cost-revenue-analytics/05-CONTEXT.md
    - .planning/phases/05-cost-revenue-analytics/05-RESEARCH.md
    - backend-go/database/models.go
    - backend-go/service/billing.go
    - backend-go/service/money.go
    - backend-go/service/image_test.go
  </read_first>
  <behavior>
    - Range parser accepts `today`, `7d`, `30d`, and `all`; empty range defaults to `7d`.
    - Summary sums `revenue_x10000`, `cost_x10000`, `profit_x10000`, and `success_image_count` from billing rows in range.
    - Trend returns rows bucketed by date string `YYYY-MM-DD` for bounded ranges and includes revenue/cost/profit/success image totals per bucket.
    - Endpoint breakdown groups by `endpoint_base_url_snapshot`, sums values, computes `profitRateBps`, and sorts by `profit_x10000 DESC, revenue_x10000 DESC`.
    - User breakdown groups by `user_id` and `user_label_snapshot`, sums values, computes `profitRateBps`, and sorts by `profit_x10000 DESC, revenue_x10000 DESC`.
  </behavior>
  <action>Create `backend-go/service/analytics.go` with DTOs `AnalyticsRange`, `AnalyticsMeta`, `BillingSummary`, `BillingTrendPoint`, `BillingEndpointRow`, and `BillingUserRow`. Implement `ParseAnalyticsRange(value string, now time.Time) (AnalyticsRange, error)` supporting exact query values `today`, `7d`, `30d`, and `all`; empty string maps to `7d`. `AnalyticsRange` must carry enough data for handlers to serialize `meta.range`, `meta.from`, `meta.to`, and `meta.moneyScale`. Implement `GetBillingSummary(rangeValue string)`, `GetBillingTrend(rangeValue string)`, `GetBillingEndpointBreakdown(rangeValue string)`, and `GetBillingUserBreakdown(rangeValue string)` using `database.DB.Model(&database.BillingRecord{})` queries only. Return all money fields as `int64` X10000 and expose `service.MoneyScale` through `AnalyticsMeta.MoneyScale`; do not return bare arrays without meta. Use `profitRateBps = profitX10000 * 10000 / revenueX10000`, and return `0` when revenue is `0`. Create `backend-go/service/analytics_test.go` with seeded `database.BillingRecord` rows verifying range filtering, sums, grouping, sorting, meta money scale, and profit-rate calculations.</action>
  <verify>
    <automated>cd backend-go && go test ./service/... -run 'Test.*Billing.*Analytics|TestParseAnalyticsRange|TestGetBilling'</automated>
  </verify>
  <acceptance_criteria>
    - `backend-go/service/analytics.go` contains `type AnalyticsMeta struct` with `MoneyScale`.
    - `backend-go/service/analytics.go` contains `func ParseAnalyticsRange(value string, now time.Time) (AnalyticsRange, error)`.
    - `backend-go/service/analytics.go` contains `func GetBillingSummary(rangeValue string)`.
    - `backend-go/service/analytics.go` contains `func GetBillingTrend(rangeValue string)`.
    - `backend-go/service/analytics.go` contains `func GetBillingEndpointBreakdown(rangeValue string)`.
    - `backend-go/service/analytics.go` contains `func GetBillingUserBreakdown(rangeValue string)`.
    - `backend-go/service/analytics.go` references `database.BillingRecord` and does not reference `database.User` or `database.Task` for aggregation.
    - `backend-go/service/analytics_test.go` verifies `MoneyScale` is exposed through analytics metadata.
    - `cd backend-go && go test ./service/... -run 'Test.*Billing.*Analytics|TestParseAnalyticsRange|TestGetBilling'` exits 0.
  </acceptance_criteria>
  <done>Backend service can aggregate billing snapshots for all chart/table blocks and supported ranges with explicit metadata for API wrappers.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add admin analytics handlers and routes</name>
  <files>backend-go/handler/admin.go, backend-go/handler/admin_analytics_test.go, backend-go/main.go</files>
  <read_first>
    - .planning/phases/05-cost-revenue-analytics/05-CONTEXT.md
    - .planning/phases/05-cost-revenue-analytics/05-RESEARCH.md
    - backend-go/handler/admin.go
    - backend-go/service/analytics.go
    - backend-go/service/money.go
    - backend-go/main.go
  </read_first>
  <behavior>
    - `GET /api/admin/analytics/summary?range=7d` returns `{ meta: { range, from, to, moneyScale }, summary: { revenueX10000, costX10000, profitX10000, successImages } }`.
    - `GET /api/admin/analytics/trend?range=7d` returns `{ meta, trend: [...] }`.
    - `GET /api/admin/analytics/endpoints?range=7d` returns `{ meta, rows: [...] }`.
    - `GET /api/admin/analytics/users?range=7d` returns `{ meta, rows: [...] }`.
    - Invalid range values return HTTP 400 with JSON `error` instead of silently falling back.
  </behavior>
  <action>Add handlers to `backend-go/handler/admin.go`: `AdminBillingSummary`, `AdminBillingTrend`, `AdminBillingEndpointBreakdown`, and `AdminBillingUserBreakdown`. Each handler reads `range` from query, calls the matching service function, and returns one of the explicit wrapper shapes from the context block: summary uses `{ "meta": meta, "summary": summary }`, trend uses `{ "meta": meta, "trend": trend }`, endpoint breakdown uses `{ "meta": meta, "rows": rows }`, and user breakdown uses `{ "meta": meta, "rows": rows }`. Every wrapper must include `meta.moneyScale` equal to `service.MoneyScale`; no analytics handler may return a bare `summary`, `trend`, or `rows` value without `meta`. Register authenticated admin routes in `backend-go/main.go`: `GET /api/admin/analytics/summary`, `GET /api/admin/analytics/trend`, `GET /api/admin/analytics/endpoints`, and `GET /api/admin/analytics/users`. Create `backend-go/handler/admin_analytics_test.go` with Gin tests verifying 200 response shape for `7d`, default range behavior for omitted range, `meta.moneyScale: 10000` on all four endpoints, and 400 for an unsupported range like `range=90d`.</action>
  <verify>
    <automated>cd backend-go && go test ./handler/... -run 'TestAdminBilling|TestAdminAnalytics'</automated>
  </verify>
  <acceptance_criteria>
    - `backend-go/handler/admin.go` contains `func AdminBillingSummary(c *gin.Context)`.
    - `backend-go/handler/admin.go` contains `func AdminBillingTrend(c *gin.Context)`.
    - `backend-go/handler/admin.go` contains `func AdminBillingEndpointBreakdown(c *gin.Context)`.
    - `backend-go/handler/admin.go` contains `func AdminBillingUserBreakdown(c *gin.Context)`.
    - `backend-go/handler/admin.go` returns `meta` in all four analytics handlers.
    - `backend-go/main.go` contains all four `/analytics/summary`, `/analytics/trend`, `/analytics/endpoints`, and `/analytics/users` admin routes.
    - `backend-go/handler/admin_analytics_test.go` asserts all four endpoint responses include `meta.moneyScale` equal to `10000`.
    - `backend-go/handler/admin_analytics_test.go` asserts invalid range returns status 400.
    - `cd backend-go && go test ./handler/... -run 'TestAdminBilling|TestAdminAnalytics'` exits 0.
  </acceptance_criteria>
  <done>Admin frontend can independently load each analytics block through authenticated endpoints with explicit response wrappers matching TypeScript clients.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin client -> analytics endpoints | Authenticated admin query parameters control reporting time range. |
| analytics service -> billing database | Financial aggregates are read from immutable billing snapshots. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04-01 | Tampering | `range` query parameter | mitigate | Accept only `today`, `7d`, `30d`, `all`, and empty default; invalid values return 400. |
| T-05-04-02 | Information Disclosure | analytics endpoints | mitigate | Register routes only under `adminAuth`; no public analytics routes. |
| T-05-04-03 | Denial of Service | `all` range aggregation | accept | Aggregates run on indexed billing snapshot columns and return grouped rows; no export or drill-down endpoints are added. |
| T-05-04-SC | Tampering | package installs | accept | No npm, pip, or cargo package installs in this plan. |
</threat_model>

<verification>
Run `cd backend-go && go test ./...`. Confirm analytics service only aggregates `database.BillingRecord`, supported ranges exactly match UI contract: `today`, `7d`, `30d`, `all`, and all four admin analytics responses include `meta.moneyScale: 10000`.
</verification>

<success_criteria>
- Summary, trend, endpoint breakdown, and user breakdown are available as independent authenticated admin APIs.
- All APIs use fixed-point money fields and expose `moneyScale: 10000` through an explicit `meta` wrapper.
- Statistics preserve historical accuracy by aggregating billing snapshots rather than current task/user/config state.
</success_criteria>

<output>
Create `.planning/phases/05-cost-revenue-analytics/05-04-SUMMARY.md` when done.
</output>
